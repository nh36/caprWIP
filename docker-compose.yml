services:
  backend:
    build:
      context: server
      dockerfile: Dockerfile
    entrypoint:
      - flask
      - run
      - --host=0.0.0.0
    environment:
      FLASK_DEBUG: 1
      FLASK_APP: server
    ports:
      - 5001:5000
    volumes:
      - ./server:/usr/app                    # Backend code
      - ./Germanic/data:/usr/app/data        # Germanic data files
      - ./Germanic/fsts:/usr/app/fsts        # Germanic FST sources
      - ./Germanic/tools:/usr/app/tools      # Germanic tools
      - ./Germanic/docs:/usr/app/docs        # Germanic docs (debug_snapshots)
    depends_on:
      - frontend
  frontend:
    build:
      context: cognate-app
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    volumes:
      - ./client:/usr/app