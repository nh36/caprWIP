# SC030 OE Au Fronting — chronology evidence card

## Current position
- current_order: `30`
- rule_name: `OEAuFronting`
- safe computational window: `30-31`
- status: `first_break_complete`

## Earlier boundary
- first earlier break: order `29`
- crossed stage: `SC029` OE Awj Glide Formation
- crossed stage type: `historical_sound_change`
- failure count: `2`
- representative failures: `hay; strew`
- concrete failure example: PGmc `*xáwwją` yields expected OE `hīeġ`, but the earlier-shifted variant yields `hauġ`; PGmc `*stráwjaną` likewise yields `strauian` instead of expected `strīeġan`
- interpretation: SC030 cannot move earlier across SC029. Pulling OE Au Fronting ahead of OE Awj Glide Formation restores pre-fronted `au` outputs where the live chronology yields fronted reflexes.

## Later boundary
- first later break: order `32`
- crossed stage: `SC032` OE Diphthong Leveling
- crossed stage type: `historical_sound_change`
- failure count: `18`
- representative failures: `believe; bow; bread; dream; flea`
- concrete failure example: PGmc `*galáubijaną` yields expected OE `ġelīefan`, but the later-shifted variant yields `+?` (no output); PGmc `*bráudą` likewise yields no output instead of expected `brēad`; PGmc `*dráugmaz` yields no output instead of expected `drēam`
- interpretation: SC030 cannot move later across SC032. Delaying OE Au Fronting past OE Diphthong Leveling causes a broad set of derivations to fail outright rather than to produce alternate surface forms.

## Chronology statement
Current first-break evidence places SC030 after `SC029` OE Awj Glide Formation and before `SC032` OE Diphthong Leveling. If OE Au Fronting is moved before `SC029`, PGmc `*xáwwją` yields `hauġ` rather than expected OE `hīeġ`, and PGmc `*stráwjaną` yields `strauian` rather than `strīeġan`. If it is moved later than `SC032`, PGmc `*galáubijaną`, `*bráudą`, and sixteen other baseline-matching derivations fail to produce output at all (`+?`) instead of yielding the expected Old English forms. The later side therefore reciprocates `SC032` earlier.

## Caveats
The later failure set must be described as a cluster of no-output / failed derivations, not as a competing set of surface Old English forms.

## Source files
- `Germanic/docs/sound_changes/order_tests/summaries/order_sensitivity_first_break_pilot_03.tsv`
- `Germanic/docs/sound_changes/order_tests/summaries/order_sensitivity_first_break_pilot_03_changes.tsv`
- `Germanic/docs/sound_changes/order_tests/summaries/order_sensitivity_first_break_pilot_03_failures.tsv`
- `Germanic/docs/sound_changes/sound_change_order_sensitivity.tsv`
