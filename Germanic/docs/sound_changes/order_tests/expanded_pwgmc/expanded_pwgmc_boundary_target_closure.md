# Expanded PWGmc boundary-target closure

## What is now closed

The narrow expanded-PWGmc boundary-target task is now closed for the **earlier-side bundled-`PWGmcChanges` problem**.

That task was defined narrowly:

1. find default-profile earlier-side first-break rows that stopped at bundled `PWGmcChanges`;
2. rerun those rows under `expanded-pwgmc`;
3. replace each opaque bundled-boundary result with either:
   - a more specific internal PWGmc break; or
   - stronger no-break evidence down to `SC004`.

That earlier-side bundled-boundary question is now exhausted.

## Why the task is considered complete

The expanded layer contains additional local and broad/far confirmations from earlier exploratory contiguous mini-batches, but those extra confirmations are **not** the reason the boundary-target task is complete.

The real closure condition is that all **23** true default earlier-side bundled-PWGmc boundary targets now have expanded-PWGmc results.

The key finding is:

1. opening bundled `PWGmcChanges` produced two specific internal positive breaks:
   - `SC031` earlier across `SC011` `PWGmc Syllabic J`
   - `SC033` earlier across `SC008` `PWGmc Coronal W Assimilation`
2. the remaining bundled-boundary cases resolved as no-break-to-`SC004` evidence, including the final four:
   - `SC050`
   - `SC065`
   - `SC067`
   - `SC076`

So the earlier-side bundled-boundary problem is closed even though the broader chronology still contains many other questions.

## What this does not mean

This closure does **not** mean:

1. that the entire chronology is finished;
2. that the default chronology-card corpus should automatically be revised;
3. that later-direction expanded-profile testing should begin automatically.

The default chronology cards, default first-break TSVs, and default graph files remain unchanged by this closure layer.

Later-direction expanded-profile testing is still out of scope unless a separate question is explicitly posed.
