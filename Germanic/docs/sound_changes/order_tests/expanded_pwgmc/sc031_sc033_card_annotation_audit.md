# SC031 / SC033 expanded-PWGmc card annotation audit

## Scope of edits

The applied expanded-PWGmc integration is narrow and matches the policy/proposal scope.

Only the two intended ordinary chronology cards now contain an `## Expanded-PWGmc supplementary note` section:

1. `Germanic/docs/sound_changes/order_tests/chronology_cards/SC031-oe-ww-simplification.md`
2. `Germanic/docs/sound_changes/order_tests/chronology_cards/SC033-oe-ew-long-diphthong.md`

No additional ordinary chronology cards were found with that section heading.

## Placement

Both cards place the new supplementary note in the intended location:

1. after `## Chronology statement`
2. before `## Caveats`

That matches the narrow insertion plan from `sc031_sc033_card_annotation_proposal.md` and preserves the existing `Earlier boundary`, `Chronology statement`, and `Caveats` text.

## Content

The applied content is also narrow and correct.

### SC031

The `SC031` card note:

1. identifies the first internal positive break as crossing `SC011` `PWGmc Syllabic J`;
2. names `hay` as the representative failure;
3. states that the expanded-PWGmc evidence supplements, but does not replace, the default bundled-profile card evidence.

### SC033

The `SC033` card note:

1. identifies the first internal positive break as crossing `SC008` `PWGmc Coronal W Assimilation`;
2. names `four` as the representative failure;
3. states that the expanded-PWGmc evidence supplements, but does not replace, the default bundled-profile card evidence.

The applied wording stays descriptive and does not intensify the historical interpretation inside the new inserted notes.

## Links

Both supplementary notes link to the intended reader-facing expanded-PWGmc documents:

1. `expanded_pwgmc_integration_policy_draft.md`
2. `expanded_pwgmc_boundary_target_closure.md`
3. `expanded_pwgmc_phase_synthesis.md`

This is the right minimal link set for ordinary cards. The machine-oriented expanded result index was not imported into the notes.

## Non-scope confirmation

This integration remained within the approved narrow scope.

Confirmed:

1. no strengthened no-break-to-`SC004` rows were imported into ordinary cards;
2. no exploratory local or broad/far contiguous-mini-batch confirmations were imported into ordinary cards;
3. no default first-break TSVs were changed;
4. no graph exports were changed;
5. no later-direction expanded-profile testing was initiated.

So the applied integration is limited to the two approved internal positive-break cases only: `SC031` and `SC033`.

## Recommendation

The `SC031` / `SC033` expanded-PWGmc card integration is complete unless Nathan wants to revise the wording stylistically.

The next project phase should now move away from expanded-PWGmc and return to the broader default chronology-card corpus. The most useful next step is either:

1. a consistency review of the ordinary card set as a whole; or
2. a policy decision about which ordinary chronology relations are strong enough to feed a graph/export layer.
