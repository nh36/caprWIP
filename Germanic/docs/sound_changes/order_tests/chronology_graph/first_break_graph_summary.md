# First-break chronology graph summary

## Scope

This graph export was generated from the audited first-break chronology corpus. It reuses the current chronology-card, index, and summary-table layer only; no new first-break computations were run.

In this documentation, **ordinary chronology** means a first-break relation between modeled sound-change rules. Technical markers, bundled runner stages such as `PWGmcChanges`, and no-break search boundaries are still useful computational evidence, but they are not ordinary sound-change chronology constraints.

## Totals

- total node count: `70`
- total edge count: `140`

### Node counts by card_type

| card_type | count |
| --- | ---: |
| `broad_far` | 9 |
| `mixed` | 14 |
| `negative_boundary` | 10 |
| `one_sided_chronology` | 5 |
| `reciprocal_or_near_reciprocal` | 31 |
| `runner_limited_or_technical` | 1 |

### Edge counts by relation_type

| relation_type | count |
| --- | ---: |
| `broad_far_chronology` | 18 |
| `near_reciprocal_chronology` | 2 |
| `no_break_search_boundary` | 23 |
| `one_sided_chronology` | 32 |
| `reciprocal_chronology` | 40 |
| `runner_limited_boundary` | 22 |
| `technical_computational` | 3 |

## Strongest reciprocal / near-reciprocal clusters

The list below includes both tight reciprocal relations and broader reciprocal-like corridors. Entries marked `broad_far_chronology` or `near_reciprocal_chronology` should not be read as immediate local adjacency claims.

1. `SC016 / SC017` — `reciprocal_chronology`; representative lexemes: `yoke`; forms: PGmc `*júką` yields expected OE `ġeoc`, but the later-shifted variant yields `ġoc`
1. `SC017 / SC019` — `reciprocal_chronology`; representative lexemes: `nose; shovel; sorrow`; forms: PGmc `*núsō` yields expected OE `nosu`, but the later-shifted variant yields `nusu`; PGmc `*skúflō` likewise yields `sċufl` instead of expected `sċofl`; PGmc `*súrgō` yields `surg` instead of expected `sorg`
1. `SC019 / SC020` — `reciprocal_chronology`; representative lexemes: `rest`; forms: PGmc `*rástōz` yields expected OE `ræste`, but the later-shifted variant yields `rast`
1. `SC026 / SC027` — `reciprocal_chronology`; representative lexemes: `fist; goose; youth`; forms: PGmc `*fúnxstiz` yields expected OE `fȳst`, but the later-shifted variant yields `fyst`; PGmc `*gánsz` likewise yields `ġeas` instead of expected `gōs`; PGmc `*júgunθ` yields `ġeogoþ` instead of expected `ġeoguþ`
1. `SC029 / SC030` — `reciprocal_chronology`; representative lexemes: `hay; strew`; forms: PGmc `*xáwwją` yields expected OE `hīeġ`, but the later-shifted variant yields `hauġ`; PGmc `*stráwjaną` likewise yields `strauian` instead of expected `strīeġan`
1. `SC030 / SC032` — `reciprocal_chronology`; representative lexemes: `believe; bow; bread; dream; flea`; forms: PGmc `*galáubijaną` yields expected OE `ġelīefan`, but the later-shifted variant yields `+?` (no output); PGmc `*bráudą` likewise yields no output instead of expected `brēad`; PGmc `*dráugmaz` yields no output instead of expected `drēam`
1. `SC031 / SC034` — `reciprocal_chronology`; representative lexemes: `dew; hew`; forms: PGmc `*dáwwō` yields expected OE `dēaw`, but the later-shifted variant yields `dawu`; PGmc `*xáwwaną` likewise yields `hawan` instead of expected `hēawan`
1. `SC039 / SC040` — `reciprocal_chronology`; representative lexemes: `widow`; forms: PGmc `*wíduwōn` yields expected OE `wuduwe`, but the later-shifted variant yields `wudowe`
1. `SC042 / SC043` — `reciprocal_chronology`; representative lexemes: `rest`; forms: PGmc `*rástōz` yields expected OE `ræste`, but the later-shifted variant yields `rasta`
1. `SC043 / SC044` — `reciprocal_chronology`; representative lexemes: `slay`; forms: PGmc `*sláxaną` yields expected OE `slēan`, but the later-shifted variant yields `sleaan | slēaan`
1. `SC044 / SC045` — `reciprocal_chronology`; representative lexemes: `fee; fight; flax; knight; laugh`; forms: PGmc `*féxu` yields expected OE `feoh`, but the later-shifted variant yields `fehu`; PGmc `*féxtaną` likewise yields `fehtan` instead of expected `feohtan`
1. `SC047 / SC048` — `broad_far_chronology`; representative lexemes: `bake; begin; believe; bind; bore`; forms: PGmc `*bákaną` yields expected OE `bacan`, but the later-shifted variant yields `bacen`; PGmc `*bíndaną` likewise yields `binden` instead of expected `bindan`
1. `SC052 / SC055` — `reciprocal_chronology`; representative lexemes: `cow; lung`; forms: PGmc `*kūi` yields expected OE `cȳ`, but the later-shifted variant yields `ċȳ`; PGmc `*lúnganjō` likewise yields `lunġen` instead of expected `lungen`
1. `SC055 / SC056` — `reciprocal_chronology`; representative lexemes: `gift; sheath`; forms: PGmc `*géftiz` yields expected OE `ġift`, but the later-shifted variant yields `ġieft`; PGmc `*skáiθiz` likewise yields `sċǣþ` instead of expected `sċēaþ`
1. `SC064 / SC072` — `near_reciprocal_chronology`; representative lexemes: `fright`; forms: PGmc `*fúrxtīnaz` yields expected OE `fyrhte`, but the later-shifted variant again yields `fyrhten`
1. `SC066 / SC068` — `reciprocal_chronology`; representative lexemes: `spindle`; forms: PGmc `*spénnilō` yields expected OE `spinl`, but the later-shifted variant yields `spinnl`
1. `SC070 / SC071` — `reciprocal_chronology`; representative lexemes: `bore (3sg); learn (3sg); lick (3sg); make (3sg); month`; forms: PGmc `*búrōθi` yields expected OE `boraþ`, but the later-shifted variant yields `boreþ`; PGmc `*mḗnōθz` likewise yields `mōneþ` instead of expected `mōnaþ`
1. `SC072 / SC073` — `broad_far_chronology`; representative lexemes: `adder; earth; father; find; flask`; forms: PGmc `*nḗdrōn` yields expected OE `nǣdre`, but the later-shifted variant yields `nǣdræ`; PGmc `*fádēr` likewise yields `fædær` instead of expected `fæder`
1. `SC074 / SC075` — `reciprocal_chronology`; representative lexemes: `shilling`; forms: PGmc `*skíllingaz` yields expected OE `sċilling`, but the later-shifted variant yields `sċilleng`
1. `SC079 / SC080` — `reciprocal_chronology`; representative lexemes: `lung`; forms: PGmc `*lúnganjō` yields expected OE `lungen`, but the later-shifted variant yields `lungenn`
1. `SC081 / SC082` — `reciprocal_chronology`; representative lexemes: `strew`; forms: PGmc `*stráwjaną` yields expected OE `strīeġan`, but the later-shifted variant yields `strīeian`
1. `SC082 / SC083` — `reciprocal_chronology`; representative lexemes: `bore; handle; learn; lick; make`; forms: PGmc `*búrōjaną` yields expected OE `borian`, but the later-shifted variant yields `boreian`; PGmc `*xándlōjaną` likewise yields `handleian` instead of expected `handlian`; PGmc `*mákōjaną` yields `maceian` instead of `macian`
1. `SC085 / SC086` — `reciprocal_chronology`; representative lexemes: `flee; slay; ten; toe`; forms: PGmc `*fléuxaną` yields expected OE `flēon`, but the later-shifted variant yields `flēoan`; PGmc `*sláxaną` likewise yields `sleaan` instead of expected `slēan`; PGmc `*téxun` yields `teoon` instead of `tēon`; PGmc `*táixōn` yields `tāe` instead of `tā`

## Broad / far chronology constraints

1. `SC015 -> SC036` (later_boundary) — lexemes: `world`; forms: PGmc `*wír-àldu` yields expected OE `weorold`, but the later-shifted variant yields `wuruld`
1. `SC020 -> SC040` (later_boundary) — lexemes: `beaver; bough; cud; field; flood`; forms: PGmc `*bébruz` yields expected OE `befer`, but the later-shifted variant yields `befro`; PGmc `*kwéðuz` likewise yields `cwedo` instead of expected `cwedu`; PGmc `*félθuz` yields `feldo` instead of `feld`
1. `SC021 -> SC040` (later_boundary) — lexemes: `heaven`; forms: PGmc `*xémonų` yields expected OE `heofon`, but the later-shifted variant yields `heofun`
1. `SC023 -> SC047` (later_boundary) — lexemes: `do`; forms: PGmc `*dōną` yields expected OE `dōn`, but the later-shifted variant yields `+?` (no output)
1. `SC024 -> SC056` (later_boundary) — lexemes: `sheep; year`; forms: PGmc `*skḗpą` yields expected OE `sċēap`, but the later-shifted variant yields `sċīep`; PGmc `*jḗrą` likewise yields `ġīer` instead of expected `ġēar`
1. `SC033 -> SC044` (later_boundary) — lexemes: `chew; four; knee`; forms: PGmc `*kéwwaną` yields expected OE `ċēowan`, but the later-shifted variant yields `ċeowan`; PGmc `*fédwōr` likewise yields `feower` instead of expected `fēower`; PGmc `*knéwą` yields `cneow` instead of `cnēow`
1. `SC036 -> SC019` (earlier_boundary) — lexemes: `soul`; forms: PGmc `*sáiwalō` yields expected OE `sāwol`, but the earlier-shifted variant yields `sāwel`
1. `SC040 -> SC072` (later_boundary) — lexemes: `youth`; forms: PGmc `*júgunθ` yields expected OE `ġeoguþ`, but the later-shifted variant yields `ġeogoþ`
1. `SC041 -> SC020` (earlier_boundary) — lexemes: `beard; bosom; bottom; calf; coat`; forms: PGmc `*bárdaz` yields expected OE `beard`, but the earlier-shifted variant yields `bearda`; PGmc `*kámbaz` likewise yields `camba` instead of expected `camb`
1. `SC047 -> SC048` (later_boundary) — lexemes: `bake; begin; believe; bind; bore`; forms: PGmc `*bákaną` yields expected OE `bacan`, but the later-shifted variant yields `bacen`; PGmc `*bíndaną` likewise yields `binden` instead of expected `bindan`
1. `SC048 -> SC047` (earlier_boundary) — lexemes: `bake; begin; believe; bind; bore`; forms: PGmc `*bákaną` yields expected OE `bacan`, but the earlier-shifted variant yields `bacen`; PGmc `*bíndaną` likewise yields `binden` instead of expected `bindan`
1. `SC069 -> SC023` (earlier_boundary) — lexemes: `adder; earth; flask; heart; line`; forms: PGmc `*nḗdrōn` yields expected OE `nǣdre`, but the earlier-shifted variant yields `nǣdran`; PGmc `*érθōn` likewise yields `eorþan` instead of expected `eorþe`; PGmc `*fláskōn` yields `flascan` instead of `flasce`
1. `SC072 -> SC073` (later_boundary) — lexemes: `adder; earth; father; find; flask`; forms: PGmc `*nḗdrōn` yields expected OE `nǣdre`, but the later-shifted variant yields `nǣdræ`; PGmc `*fádēr` likewise yields `fædær` instead of expected `fæder`
1. `SC073 -> SC072` (earlier_boundary) — lexemes: `adder; earth; father; find; flask`; forms: PGmc `*nḗdrōn` yields expected OE `nǣdre`, but the earlier-shifted variant yields `nǣdræ`; PGmc `*fádēr` likewise yields `fædær` instead of expected `fæder`
1. `SC078 -> SC070` (earlier_boundary) — lexemes: `bake; begin; believe; bind; bore`; forms: PGmc `*bákaną` yields expected OE `bacan`, but the earlier-shifted variant yields `bacen`; PGmc `*bíndaną` likewise yields `binden` instead of expected `bindan`
1. `SC079 -> SC055` (earlier_boundary) — lexemes: `believe; bow; fast; follow; heal`; forms: PGmc `*galáubijaną` yields expected OE `ġelīefan`, but the earlier-shifted variant yields `ġelēafan`; PGmc `*báugijaną` likewise yields `bēaġan` instead of expected `bīeġan`; PGmc `*fúlgijaną` yields `fulġan` instead of `fylġan`
1. `SC081 -> SC055` (earlier_boundary) — lexemes: `strew`; forms: PGmc `*stráwjaną` yields expected OE `strīeġan`, but the earlier-shifted variant yields `strēaġan`
1. `SC087 -> SC044` (earlier_boundary) — lexemes: `burst`; forms: PGmc `*bréstaną` yields expected OE `berstan`, but the earlier-shifted variant yields `beorstan`

## Negative / boundary-only nodes

`SC014`, `SC018`, `SC022`, `SC025`, `SC028`, `SC053`, `SC058`, `SC065`, `SC067`, `SC076`

## Runner-limited and technical / boundary cases

- earlier runner-limited boundaries to `PWGmcChanges`: `SC014`, `SC015`, `SC016`, `SC018`, `SC021`, `SC022`, `SC023`, `SC024`, `SC025`, `SC026`, `SC028`, `SC029`, `SC035`, `SC037`, `SC039`, `SC050`, `SC053`, `SC058`, `SC065`, `SC067`, `SC076`
- later no-break search-boundary cases before `SC087`: `SC014`, `SC018`, `SC022`, `SC025`, `SC027`, `SC028`, `SC049`, `SC053`, `SC056`, `SC057`, `SC058`, `SC060`, `SC061`, `SC065`, `SC067`, `SC068`, `SC069`, `SC071`, `SC075`, `SC076`, `SC080`, `SC083`, `SC086`
- these runner-limited and no-break edges are diagnostic search-boundary observations, not ordinary sound-change chronology constraints tying the source rule to `PWGmcChanges` or `SC087`
- technical / scaffolding edges:
  - `SC031 -> PWGmcChanges` (earlier_boundary) — Earlier computational break crosses bundled PWGmcChanges and is technical/scaffolding; later boundary reciprocates SC034 earlier around dew and hew.
  - `SC033 -> PWGmcChanges` (earlier_boundary) — Earlier computational break crosses bundled PWGmcChanges and is technical/scaffolding; later boundary is an ordinary chronology boundary but broad/far across SC044.
  - `SC037 -> SC038` (later_boundary) — Earlier side has no ordinary chronology break before bundled PWGmcChanges; later computational break crosses technical-marker SC038 and is technical/scaffolding.

## Recommendation for the next phase

Use this export as the data layer for a visualization pass first: the node categories and relation types are now explicit enough to support static diagrams or chapter-planning views without reinterpreting the cards by hand. After that, the highest-value technical follow-up remains runner work that exposes bundled stages such as `PWGmcChanges` so the current runner-limited earlier boundaries can be turned into ordinary chronology targets.
