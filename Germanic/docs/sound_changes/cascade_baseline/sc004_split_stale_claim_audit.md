# SC004 split: stale-claim audit

Branch `historical-cascade-order`; FST split commit `f59b758d`; PROTOFORM
correction commit `9c71aed3`.

> **Corrected PROTOFORM pass.** An earlier version of this audit described SC004
> as "general `*ai/*ái > *ā`" and SC014 as "corpus-inert." Both are now corrected:
> under the production `PROTOFORM`, **SC004 is stressed/root `*ái > *ā` only**
> (24 corpus rows) and **SC014 is unstressed `*ai > *ē`** (final and nonfinal)
> with **two corpus witnesses, span and meed** (not corpus-inert). `loam` is a
> stressed SC004 case; `whine`/`withy` are not ai cases. See
> `sc004_component_application_report.tsv`.

Per task §9, this note reports every remaining repository occurrence of the five
pre-split claim patterns and records, for each, whether it was **updated** or is
**retained as historical audit material** (or is a **generated artefact** pending
downstream reassembly). The five patterns are:

1. SC004 is Proto-West Germanic;
2. SC004 includes final `*-ai > *-ē`;
3. SC014 rewrites `*ăi`;
4. SC014 is merely a no-op;
5. the SC036 boundary applies to the final component.

## Updated (authoritative sources)

These now describe the split correctly (SC004 = stressed/root `*ái > *ā`, EAF /
North Sea Germanic; SC014 = unstressed `*ai > *ē`, PNWGmc, corpus witnesses
span and meed):

- `sound_change_inventory.tsv`, `sound_change_historical_staging_map.tsv`,
  `sound_change_aliases.tsv`;
- `cascade_baseline/cascade_order_manifest.tsv`,
  `cascade_baseline/historical_partial_order.tsv`,
  `cascade_baseline/cascade_interaction_matrix.tsv`,
  `cascade_baseline/cascade_interaction_report.md`;
- `order_tests/chronology_cards/chronology_card_index.tsv`,
  `order_tests/chronology_cards/SC004-pwgmc-ai-monophthongization.md`,
  `order_tests/chronology_cards/SC014-nwgmc-unstressed-ai-monophthongization.md`,
  `order_tests/chronology_cards/SC004-components-chronology.md`;
- literature/change/book dossiers for `004-pwgmc-ai-monophthongization` and the
  SC014 portions of `014-015-opening-vowel-prelude`;
- reader-facing `004-pwgmc-ai-monophthongization.md`,
  `014-015-opening-vowel-prelude.md` (SC014 section), `chap3-pwgmc-to-af-intro.md`;
- `fsts/germanic.txt`, `fsts/old_english_sandbox.txt`, and the trace tools.

## Retained as historical audit material

These record completed prior campaigns run against the **pre-split bundled
cascade**, where SC004 `PWGmcAiMonophthongization` opened the pipeline at order 4
and SC014 was a `{*ăi} -> {*ē}` no-op. Re-running those campaigns is out of scope
for the SC004 split (it would not change the frozen corpus outputs), so the
records are retained as-is:

- `order_tests/expanded_pwgmc/*.md`, `order_tests/expanded_pwgmc/*.tsv`,
  `order_tests/summaries/order_sensitivity_first_break_*`,
  `order_tests/expanded_pwgmc_smoke_pilot_01.md`,
  `order_tests/expanded_pwgmc_runner_mode.md`,
  `order_tests/first_break_batch_plan_04.md`: first-break order-test campaign
  records; they cite `SC004 PWGmc Ai Monophthongization` as the order-4 left
  boundary of the tested chain. The SC036 `soul` break they record is real and
  still belongs to SC004's general component (the split does not move SC004
  across SC036).
- `cascade_baseline/historical_audit_table.tsv`,
  `cascade_baseline/historical_audit_evidence_inventory.tsv`: rename-migration
  audit records; the SC014 rows still show the pre-split `{*ăi} -> {*ē}`
  definition (and, in the evidence inventory, the pre-Task-A identifier
  `NWGmcUnstressedAiMonophthongization`). Retained as a record of that audit.
- `cascade_baseline/post_rename_integrity_report.md`,
  `tools/build_rename_migration_manifest.py`: Task-A/B migration records.
- `cascade_baseline/cascade_order_manifest_frozen.tsv`: the immutable
  post-rename baseline; the split is a deliberate, documented departure from it.
- Split-note references to the former `{*ăi} -> {*ē}` no-op inside the updated
  cards/dossiers/report (they explain what was removed).

### Not a match

`*mannăi`, `*spannăi`, and the `-ăi` weak-tail ending discussed in
`DEV_NOTES.md`, `prosodic_tier_research.md`, `debug_snapshots/*`, and the lexeme
tables are the **dative-singular reconstructed ending** `*-ăi`, i.e. genuine
proto-form data, not the SC014 rule. They are left unchanged.

## Generated artefacts pending downstream reassembly

These are assembled from the reader-facing sources and the FST, and still carry
the old SC004 reader content (`define PWGmcAiMonophthongization`, the `{*ăi}`
no-op, and the "Proto-West-Germanic ai-monophthongization" heading). They are
regenerated by the reader-facing / book / lexical Docker build pipeline, which
consumes the updated sources:

- `reader_facing/reader_facing_local_section_14..19.md` (+ their `_report.md`);
- `assembly/capr_book_draft_alpha_01.md` (+ `.pdf`);
- `assembly/lexical_volume_alpha_01.md`,
  `assembly/lexical_volume_regular_compact_alpha_01.md`;
- the index-verborum tables/audits derived from the assembled book.

Frozen invariants confirmed unchanged by the split: corpus `outputs_sha256`
`aaf19ba919cafbe86ea59d482ce74d0944f541336e246da481a3f37b20da480e`
(380 accepted, 372 matched, 8 mismatched) and the mismatch report
(`debug_snapshots/oe_mismatch_report.txt`, byte-identical after regeneration).
