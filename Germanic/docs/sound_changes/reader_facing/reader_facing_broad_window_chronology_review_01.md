# Reader-facing broad-window chronology review 01

## Latest commit inspected

1. `f1ae74e6 docs: ground source-based chronology prose`

## Rules reviewed

1. `SC020`
2. `SC034`
3. `SC036`
4. `SC037`
5. `SC040`
6. `SC041`
7. `SC042`
8. `SC045`
9. `SC047`
10. `SC048`
11. `SC054`
12. `SC059`
13. `SC063`
14. `SC064`
15. `SC066`
16. `SC070`
17. `SC072`
18. `SC073`
19. `SC078`
20. `SC079`
21. `SC081`

## Rules edited

1. `SC020` — `Germanic/docs/sound_changes/reader_facing/019-020-final-long-o-raising-and-final-z-deletion.md`
2. `SC034` — `Germanic/docs/sound_changes/reader_facing/031-034-west-saxon-diphthong-chain.md`
3. `SC036` — `Germanic/docs/sound_changes/reader_facing/035-037-prefix-and-compound-adjustments.md`
4. `SC037` — `Germanic/docs/sound_changes/reader_facing/035-037-prefix-and-compound-adjustments.md`
5. `SC042` — `Germanic/docs/sound_changes/reader_facing/042-surviving-bimoric-o-unrounding.md`
6. `SC054` — `Germanic/docs/sound_changes/reader_facing/053-054-pre-umlaut-bridge-and-w-loss.md`
7. `SC059` — `Germanic/docs/sound_changes/reader_facing/059-oe-back-mutation.md`
8. `SC064` — `Germanic/docs/sound_changes/reader_facing/064-065-post-apocope-tail.md`
9. `SC066` — `Germanic/docs/sound_changes/reader_facing/066-068-syncope-and-degemination-corridor.md`
10. `SC070` — `Germanic/docs/sound_changes/reader_facing/070-071-early-unstressed-fronting-shortening-bridge.md`
11. `SC073` — `Germanic/docs/sound_changes/reader_facing/072-073-unstressed-long-vowel-shortening-and-ae-merger-core.md`
12. `SC078` — `Germanic/docs/sound_changes/reader_facing/078-weak-tail-reduction.md`
13. `SC081` — `Germanic/docs/sound_changes/reader_facing/081-083-j-strengthening-vocalization-and-ei-contraction.md`

## Rules left unchanged, with reason

1. `SC040` already says that the later relation to `SC072` is broader than the local `SC039`/`SC040` pair and does not overclaim adjacency.
2. `SC041` already distinguishes the broad left boundary from the nearer right boundary and already explains why the rule belongs between medial lowering and restoration.
3. `SC045` already treats the `SC060` relation as a forward cross-reference and keeps the local center on `SC044`.
4. `SC047` already says that its two sides are not equally local and does not pretend that the earlier `SC034` relation is a close local pair.
5. `SC048` already keeps the local center on `SC047` while treating `SC059` as the farther outward relation.
6. `SC063` already explains that apocope is late but not final in the weak-tail sequence and already frames `SC072` as a later stage rather than an adjacent partner.
7. `SC072` already treats `SC073` as the local core while keeping `SC064` and `SC085` as outward relations.
8. `SC079` already distinguishes its broad earlier relation to `SC055` from its close local relation to `SC080`.

## Edited rules: what the order test actually shows and why the prose changed

1. `SC020`
   - order test: `SC019 < SC020 < SC040`, with a local `rest` relation on the left and a much wider later constraint at `SC040`
   - old prose issue: it acknowledged the broad later side, but it still needed a clearer statement that the modeled slot is inside a wider final-syllable interval
   - placement rationale now stated: the rule stays here because the handbooks treat final `*z`-loss as part of the same West Germanic ending history that follows final `*ō`-raising
2. `SC034`
   - order test: `SC031 < SC034 < SC043`, with the local reciprocal `dew` / `hew` seam on the left and a broader forward limit at `SC043`
   - old prose issue: it stated both failures but did not say that the later edge is an interval limit rather than a close neighboring constraint
   - placement rationale now stated: the rule stays here because the `dēaw` / `hēawan` / `sċēawian` material belongs to the West Saxon diphthong zone before brightening
3. `SC036`
   - order test: `SC019 < SC036 < SC040`, with the broader left edge on `soul` and the nearer local right edge on `soul` / `world`
   - old prose issue: it admitted that the earlier edge reached back, but it did not yet say plainly that the modeled slot is a source-based choice inside a wider low-stress interval
   - placement rationale now stated: the rule stays here because the handbooks treat `world`- and `soul`-type medial vowels as one historical grouping
4. `SC037`
   - order test: no ordinary historical positive boundary; the only live later break is against technical `SC038`
   - old prose issue: it still leaned on the `SC038` result too directly and kept older technical terminology in the reader-facing paragraph
   - placement rationale now stated: the chapter now says explicitly that `SC038` is only implementation structure, while the historical placement rests on compound-juncture reduction and unstable linking-vowel behavior in the sources
5. `SC042`
   - order test: `SC020 < SC042 < SC043`, both sides carried only by `rest`
   - old prose issue: “exact on both sides” risked overselling the broad left relation to `SC020`
   - placement rationale now stated: the rule is a short context note on the left edge of brightening, not a large independent chapter
6. `SC054`
   - order test: `SC020 < SC054 < SC063`, both sides defined by the same `sea` derivation
   - old prose issue: it could be read as if the rule were tightly pinned immediately beside both of those stages
   - placement rationale now stated: the rule stays here because the sources treat loss of `*w` before unstressed `*i` as pre-umlaut history behind `sǣ`
7. `SC059`
   - order test: `SC048 < SC059 < SC078`, with the broader right edge pointing into later weak-tail reduction
   - old prose issue: “real local seam” was too strong for a rule whose later boundary is an outward interval limit
   - placement rationale now stated: the rule stays here because the handbooks treat back mutation as a distinct later vowel development between earlier mutation/restoration material and the closing weak-tail cleanup
8. `SC064`
   - order test: `SC041 < SC064 < SC072`, both sides carried by the same `fright` witness
   - old prose issue: it needed a clearer statement that the two-sided evidence still defines a wide post-apocope interval rather than a close local pair
   - placement rationale now stated: the rule stays here because the `fright` family belongs to that broader weak-tail aftermath
9. `SC066`
   - order test: `SC055 < SC066 < SC068`, with the earlier side farther back and the later side locally tied to `spindle`
   - old prose issue: it could still sound as though both sides fixed equally close neighbors
   - placement rationale now stated: the rule stays here as the opening step in the syncope-and-cluster-simplification sequence
10. `SC070`
    - order test: `SC052 < SC070 < SC071`, with the local hinge on the right and the broader earlier tie to older palatal developments
    - old prose issue: the old sentence understated how far back the earlier constraint reaches
    - placement rationale now stated: the rule stays here as an early bridge inside the later unstressed-vowel sequence
11. `SC073`
    - order test: `SC072 < SC073 < SC085`, with the close local pair on the left and the broader outward limit on the right
    - old prose issue: it reversed the relative breadth of the two sides and risked making `SC085` sound like an adjacent partner
    - placement rationale now stated: the pair stays compact because `SC072` fixes the local seam while `SC085` is only the later closing handoff
12. `SC078`
    - order test: `SC070 < SC078 < SC086`, with a very wide earlier interval and a much narrower later edge
    - old prose issue: it still used interval-limit wording that sounded too internal for reader-facing prose
    - placement rationale now stated: the chapter now describes the earlier side as a broad diagnostic range and keeps the later edge as the clearer right-hand relation
13. `SC081`
    - order test: `SC055 < SC081 < SC082`, with the broad left edge and the local `strew` seam on the right
    - old prose issue: it still used “broad and far” language and needed a clearer distinction between the interval on the left and the local chain on the right
    - placement rationale now stated: the rule stays here as the left flank of the `SC081-SC083` chain

## SC037 technical-marker review result

1. The chronology card still shows only a technical break at `SC038`.
2. The revised chapter now says plainly that this is implementation structure rather than ordinary historical evidence.
3. The historical rationale for `SC037` now rests on compound-juncture weakening and unstable linking vowels in the sources, not on treating `SC038` as a real sound-change neighbor.

## Remaining broad-window cases needing deeper source research

1. No broad-window case in this pass required immediate deeper literature research to support a one- or two-sentence correction.
2. The more substantial unresolved questions are architectural rather than documentary: how far some of these grouped chapters should later split around their strongest local cores.
3. If later prose work wants still sharper wording for the widest interval cases, the most likely follow-up candidates are `SC020`, `SC059`, and `SC078`, because their outer constraints reach farthest beyond the local chapter centers.

## Checks run and results

1. `bash Germanic/docs/sound_changes/reader_facing/build_reader_facing_local_section_19_docker.sh`
   - passed
   - regenerated `reader_facing_local_section_19.md`
   - regenerated `reader_facing_local_section_19.pdf`
   - regenerated `reader_facing_manifest_coverage_07.md`
2. `python3 Germanic/docs/sound_changes/reader_facing/check_reader_facing_style.py`
   - no warnings
3. `python3 Germanic/docs/sound_changes/reader_facing/check_reader_facing_citations.py`
   - files checked: `54`
   - citation issues: `0`
4. `python3 Germanic/docs/sound_changes/reader_facing/check_reader_facing_section_order.py --build-script Germanic/docs/sound_changes/reader_facing/build_reader_facing_local_section_19_docker.sh`
   - passed
5. `python3 Germanic/docs/sound_changes/reader_facing/check_reader_facing_crossrefs.py --build-script Germanic/docs/sound_changes/reader_facing/build_reader_facing_local_section_19_docker.sh`
   - files checked: `54`
   - links checked: `507`
   - issues: `0`
6. `python3 Germanic/docs/sound_changes/reader_facing/check_reader_facing_chronology_evidence.py`
   - sections checked: `85`
   - warnings: `0`
7. `python3 Germanic/docs/sound_changes/reader_facing/check_reader_facing_generated_prose.py --build-script Germanic/docs/sound_changes/reader_facing/build_reader_facing_local_section_19_docker.sh`
   - paragraphs checked: `5`
   - issues: `0`
8. `git diff --check`
   - passed

## Scope confirmations

1. No new chapters were added.
2. No local section 20 was created.
3. No manifest files were changed.
4. No FST rules were changed.
5. No lexical TSV data were changed.

## Handoff for the next task

1. The corrected broad-window paragraphs now distinguish historically real rules, broad diagnostic intervals, and source-based placement choices inside those intervals.
2. Several broad-window chapters were already adequately phrased and were left untouched, especially `SC040`, `SC041`, `SC045`, `SC047`, `SC048`, `SC063`, `SC072`, and `SC079`.
3. `SC037` no longer leans on the technical-marker relation as if it were an ordinary historical boundary.
4. The next chronology-prose pass should examine the remaining one-sided cases that still use older boundary/search phrasing or otherwise overstate how tightly the order is fixed.
