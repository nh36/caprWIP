# High-vowel apocope

### Sound-change report

#### Historical formulation

This singleton scaffold keeps `SC063` visible as high-vowel apocope, already a
clear candidate for later promotion from scaffold to finished production prose.
It remains scaffolded here because the present task is to expose the whole
sound-change half, not to polish one more chapter.

#### Current evidence

`SC063` already has a chronology card, a FOMA implementation, a literature
dossier, and a book dossier. As with `SC043`, the source base here is stronger
than in most of the remaining scaffold corpus.

#### Place in the cascade

`SC063` sits between `SC061` and `SC064` in the current order and anchors the
high-vowel apocope region. The assembled scaffold therefore keeps it as a
singleton rather than folding it into a broader miscellaneous cluster.

#### Order evidence

The chronology card already behaves like a genuine local production candidate.
That makes this entry visibly near-promotion even though it is still marked as
scaffold for the present whole-half pass.

#### Status

This is a scaffold placeholder, not finished book prose. The next step is human
review and a promotion decision, not another round of local polishing inside
this scaffold build.
