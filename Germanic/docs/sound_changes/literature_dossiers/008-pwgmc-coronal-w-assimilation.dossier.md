# SC008 Coronal-w assimilation — literature dossier

## Historical phenomenon

This dossier concerns the assimilation of coronal sequences before `*w`, yielding `*ww`, especially in `four` and in plural-pronominal forms such as `you` and `your`.

## CAPR rule

- change_id: `SC008`
- display_name: `PWGmc Coronal W Assimilation`
- rule_name: `PWGmcCoronalWAssimilation`
- FOMA definition: `*d -> *w / _ *w` and `*z -> *w / _ *w`

## Example lexemes

1. `four`

## Source support found so far

1. Ringe and Taylor describe `*zw` and `*dw` assimilating to `*ww` as a shared Proto-West-Germanic innovation [@RingeTaylor2014, pp. 56--57].
2. Their historical examples are `four`, `you (dat.pl.)`, and `your (pl.)` [@RingeTaylor2014, pp. 56--57].

## Source gaps

1. The historical evidence is compelling but concentrated in very few examples.
2. The present compact trace foregrounds `four`, while the pronominal support remains source-facing rather than trace-heavy.

## Chronology/order-test status

1. A validated chronology card now exists from the expanded-PWGmc first-break output family.
2. The earlier search moved safely across SC007, SC006, SC005, and SC004 to order `4` and then reached the left edge of the tested expanded-PWGmc chain with no real break, so the earlier side is boundary-only.
3. The later search breaks at `SC031` OE WW Simplification: `*fédwōr` yields `fēowwer` instead of `fēower`.
4. That later boundary is historically interpretable but broad/far rather than tightly local.

## Cautions for future reader-facing prose

1. Keep the narrow lexical base visible.
2. Do not overread the broad formal implementation as if the sources supported a large open-ended class.
3. If later promoted, the pronominal evidence should be explained explicitly rather than hidden behind the single `four` trace witness.
