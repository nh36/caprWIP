# SC069-SC078 Late unstressed-tail cluster -- literature dossier

## Rule metadata

- change_ids: SC069; SC070; SC071; SC072; SC073; SC074; SC075; SC076; SC078
- display_names: OE Early O Shortening; OE Unstressed Fronting Early; OE Late O Shortening; OE Unstressed Long Vowel Shortening; OE Unstressed AE Merger; OE Med Unstressed I Lowering1; OE Med Unstressed I Lowering; OE Prefix I Reduction; OE Weak Tail Reduction
- FOMA rule names: `OEEarlyOShortening`; `OEUnstressedFrontingEarly`; `OELateOShortening`; `OEUnstressedLongVowelShortening`; `OEUnstressedAEMerger`; `OEMedUnstressedILowering1`; `OEMedUnstressedILowering`; `OEPrefixIReduction`; `OEWeakTailReduction`
- current_orders: 69; 70; 71; 72; 73; 74; 75; 76; 77
- chronology_cards:
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC069-oe-early-o-shortening.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC070-oe-unstressed-fronting-early.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC071-oe-late-o-shortening.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC072-oe-unstressed-long-vowel-shortening.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC073-oe-unstressed-ae-merger.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC074-oe-med-unstressed-i-lowering1.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC075-oe-med-unstressed-i-lowering.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC076-oe-prefix-i-reduction.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC078-oe-weak-tail-reduction.md`
- representative failures / examples from the cards:
  - SC069: `adder`; `earth`; `flask`; `heart`; `line`
  - SC070: `lung`; `bore (3sg)`; `month`
  - SC071: `bore (3sg)`; `learn (3sg)`; `month`
  - SC072: `fright`; `adder`; `father`
  - SC073: `adder`; `father`; `toe`
  - SC074: `fright`; `shilling`
  - SC075: `shilling`
  - SC076: no positive representative failure; both tested sides are runner-limited
  - SC078: `bake`; `bind`; `flee`; `slay`
- local chronology summary:
  - `SC069` has one broad/far earlier boundary at `SC023`; its later side is runner-bounded.
  - `SC070` is the first real local hinge in the row: `SC052 < SC070 < SC071`.
  - `SC071` is one-sided in current testing and must follow `SC070`.
  - `SC072` is the strongest internal pivot: `SC064 < SC072 < SC073`.
  - `SC073` directly reciprocates `SC072` and also points forward to `SC085`.
  - `SC074` is a narrow reciprocal bridge between `SC072` and `SC075`.
  - `SC075` is one-sided and must follow `SC074`.
  - `SC076` has no positive first-break boundary in either direction.
  - `SC078` is a broad right-edge hinge: it must follow `SC070` and precede `SC086`.

## Historical problem

`SC069-SC078` is currently a visibility scaffold rather than a historically
coherent nine-change chapter. It sits after the promoted `SC066-SC068`
late weak-tail syncope corridor and before the still-scaffolded
`SC079-SC087` closing cluster, so the book needs an explicit placeholder here.
But the grouped row mixes at least six different evidence profiles:

1. a broad/far opener (`SC069`);
2. an early local fronting/shortening seam (`SC070-SC071`);
3. a stronger shortening/merger core (`SC072-SC073`);
4. a narrow medial-`i` lowering bridge (`SC074-SC075`);
5. a negative but source-backed prefix note (`SC076`);
6. a right-edge weak-tail reduction hinge (`SC078`).

The central dossier question is therefore not whether the region exists, but how
it should later be split into smaller production-ready units without forcing one
uneven placeholder into a false chapter center.

## Source dossier

### Campbell 1959
- source_key: Campbell1959
- locator: §§345--354; §§388--389
- witness_used: `docs/references/campbell_old_english_grammar.txt`
- locator_confidence: section_safe
- terminology: loss of unaccented vowels; vowels of prefixes; early OE shortening of unaccented long vowels; loss of medial vowels after short syllables
- short quotation:

  > "Medial unaccented vowels are very freely dropped after short syllables ..."

- paraphrase: Campbell gives strong support for the broad late unstressed-vowel region but not for the present nine-member grouping as such. His sections on early OE loss of unaccented vowels and early shortening of unaccented long vowels support the general terrain behind `SC069-SC075` and `SC078`, while the explicit "Vowels of prefixes" section gives direct historical footing for `SC076` as a real late prefix-vowel phenomenon [@Campbell1959, §§345--354, 388--389].
- conditioning: final-vowel loss, shortening of unaccented long vowels, prefix-vowel weakening, and later medial-vowel loss all belong to one late weak-tail neighborhood
- chronology: Campbell supports a staged late reduction zone after the earlier umlaut and back-mutation material, but he does not flatten that zone into one chapter-sized `SC069-SC078` unit
- examples: `weorod`; `heafodu`; `betra`; `micle`; prefix vowels in unstressed formations
- cautions: Campbell is best evidence for the broad neighborhood and for `SC076`'s historical legitimacy, not for treating SC069-SC078 as one ready-made report

### Hogg 1992
- source_key: Hogg1992
- locator: pp. 120--121
- witness_used: `docs/references/hogg_vol1.txt`
- locator_confidence: page_safe
- terminology: apocope; syncope; reduction of unstressed vowels; simplification in consonant groups
- short quotation:

  > "Apocope affected the high vowels / i / and / u / ..."
  >
  > "The high vowels were also subject to syncope in medial positions after a heavy syllable ..."

- paraphrase: Hogg is especially useful for the internal weighting of the row. He treats apocope and syncope as neighboring but distinct late processes, shows how `weorod`, `heafodu`, and `heafdu` reflect their overlap, and then immediately notes that syncope-created consonant clusters can trigger later simplification [@Hogg1992, pp. 120--121]. That supports `SC072-SC075` and `SC078` as genuine late weak-tail material, while also warning against narrating every CAPR step as a separate textbook law.
- conditioning: heavy-syllable and trisyllabic apocope, medial syncope after heavy syllables, and downstream consonant-cluster simplification all belong to the same late zone
- chronology: later than i-mutation; apocope and syncope are partly overlapping rather than a flat one-rule ladder
- examples: `fet`; `word`; `scipu`; `weorod`; `heafodu`; `heafdu`
- cautions: Hogg does not isolate SC069, SC070, SC071, or SC076 as standalone handbook headings; his support is strongest for the broader shortening/syncope/reduction environment

### Ringe and Taylor 2014
- source_key: RingeTaylor2014
- locator: §§6.7.3--6.8.4; §§6.9.1--6.9.3
- witness_used: `docs/references/ringe_taylor_linguistic_history_vol2.txt`
- locator_confidence: section_safe
- terminology: general syncope of short vowels; apocope of short high vowels; shortening of unstressed long vowels; changes after apocope
- short quotation:

  > "After general syncope had run its course, short *i and *u were lost word-finally ..."
  >
  > "Apocope and the shortening of unstressed long vowels are the last prehistoric OE sound changes."

- paraphrase: Ringe and Taylor provide the clearest staged framework for the row. Their sequence of syncope, apocope, shortening of unstressed long vowels, and post-apocope changes supports the strongest internal hinge at `SC072-SC073`, while also making `SC078`'s forward relation to the closing cluster historically intelligible rather than ad hoc [@RingeTaylor2014, §§6.7.3--6.8.4, 6.9.1--6.9.3]. They are the best source for treating the middle of the row as a late-vowel-shortening/reduction system instead of as nine unrelated technical labels.
- conditioning: general syncope feeds apocope; shortening of unstressed long vowels follows; later post-apocope changes include contraction and related closing phenomena
- chronology: the row belongs after the already promoted SC063/SC064-SC068 weak-tail material, but before or alongside some of the closing phenomena now grouped in `SC079-SC087`
- examples: `mearc`; `heord`; `firen`; `ricu`; post-apocope and contraction material in §§6.9.1--6.9.3
- cautions: Ringe and Taylor strengthen `SC072-SC073` and `SC078` much more than `SC069`, `SC070-SC071`, or `SC076`; the source supports a staged zone, not the current grouped row unchanged

### Luick 1914
- source_key: Luick1914
- locator: §§304--307
- witness_used: `docs/references/luick_historische_grammatik.txt`
- locator_confidence: section_safe
- terminology: `i-, u-Schwund`; `Apokope`; `Synkope` in offener Mittelsilbe
- short quotation:

  > "Im Auslaut schwanden i und u unmittelbar nach langer Tonsilbe ..."
  >
  > "Unter denselben Bedingungen ... schwanden auch i und u in offener Mittelsilbe ..."

- paraphrase: Luick is valuable because he preserves the older grammar logic behind the split map. He distinguishes final loss from open-medial syncope while keeping both in the same late unstressed-vowel environment, which supports a later structural break between the stronger `SC072-SC073` shortening/merger core and the narrower `SC074-SC075` medial-lowering bridge [@Luick1914, §§304--307].
- conditioning: final and open-medial high-vowel loss track stress and syllable weight, but they need not be identical operations
- chronology: Luick supports a late-vowel-reduction neighborhood with internal differentiation, not a single monolithic cluster
- examples: `flōd`; `word`; `werod`; open-medial syncope material
- cautions: Luick supports the region at the level of history and chapter shape rather than the exact CAPR member boundaries

### Fulk 2018
- source_key: Fulk2018
- locator: §§5.6--5.7
- witness_used: `docs/references/fulk_comparative_grammar_early_germanic.vision.txt`
- locator_confidence: section_safe
- terminology: later preliterary changes of medial and final vowels; vowels in prefixes
- short quotation:

  > "Later the vowels in unstressed prefixes weaken ..."

- paraphrase: Fulk is the key source for preventing `SC076` from disappearing into mere technical scaffolding. His late-preliterary weak-vowel sections support the broader chronology of `SC072-SC075` and `SC078`, while the explicit prefix-vowel discussion makes `SC076` historically legible even though its chronology card is negative on both sides [@Fulk2018, §§5.6--5.7].
- conditioning: later unstressed-vowel weakening affects both medial/final vowels and unstressed prefixes
- chronology: prefix weakening belongs to the same late reduction neighborhood but is not guaranteed to line up with the local chronology seams of `SC073-SC075`
- examples: unstressed `ge-`; `of-`; `op-`; `blinnan`; `būtan`
- cautions: Fulk supports the prefix note and the broader late setting, not a nine-change production chapter

## Existing project framing

The surrounding promoted material already constrains the honest reading of this
row.

1. `SC059` is now promoted as a singleton back-mutation center whose later
   cross-reference points into this region through `SC078`, not into a finished
   corridor [@Campbell1959, §207; @RingeTaylor2014, §6.9.4; @Fulk2018, §4.8].
2. `SC063`, `SC064-SC065`, and `SC066-SC068` already articulate a staged
   weak-tail region on the left: apocope, a modest post-apocope tail, then a
   promoted syncope/cleanup corridor [@Campbell1959, §§345--349, 388--389;
   @Hogg1992, pp. 120--121; @RingeTaylor2014, §§6.7.3--6.8.4; @Fulk2018,
   §5.6].
3. The closing `SC079-SC087` scaffold remains broad on the right, so `SC078`
   cannot yet be narrated as though its later `SC086` boundary had already been
   absorbed into a mature closing chapter.

That means the dossier should judge this row by its own internal structure, not
as a substitute for either neighboring promoted zone.

## Member-by-member source notes

### SC069 `OEEarlyOShortening`

This is historically legible as part of the larger late unstressed-vowel cleanup,
but the sources do not isolate it as a handbook center. Its card is one-sided and
its only positive boundary (`SC023 < SC069`) is broad and far away. Source work
therefore supports keeping SC069 visible, not promoting it as the first
subcluster anchor.

### SC070 `OEUnstressedFrontingEarly`

SC070 is the first genuinely local member in the row. Source work supports it as
part of the broader unstressed-vowel leveling tradition, while the card gives it
real two-sided chronology (`SC052 < SC070 < SC071`). That makes it useful as an
internal bridge, though not yet as the strongest production target in the region.

### SC071 `OELateOShortening`

SC071 is historically legible but asymmetrical. The `SC070 < SC071` relation is
real and local, yet the later side is runner-bounded. The source tradition does
not give SC071 a chapter-sized independent identity; it reads more naturally as
the follower in a short `SC070-SC071` bridge.

### SC072 `OEUnstressedLongVowelShortening`

This is the strongest middle member of the row. Ringe and Taylor give it the
clearest handbook footing, and the card provides real two-sided chronology
(`SC064 < SC072 < SC073`). If the project wants the first genuinely production-
ready subcluster inside the row, SC072 is the most credible center.

### SC073 `OEUnstressedAEMerger`

SC073 is also strong, but in a different way. It directly reciprocates SC072 on
the left and points forward to `SC085` on the right. That makes it historically
real and book-useful, but it also means its later relation is already tugging
toward the closing cluster rather than staying purely internal to a middle
subchapter.

### SC074 `OEMedUnstressedILowering1`

SC074 has a neat two-sided card (`SC072 < SC074 < SC075`), but the evidence is
narrow and witness-concentrated. The source tradition supports medial weak-vowel
lowering in the broader sense, yet SC074 looks more like a modest bridge member
than like a chapter center.

### SC075 `OEMedUnstressedILowering`

SC075 is the follower to SC074. It has one real local left boundary and no later
positive break. Source work supports keeping the SC074-SC075 pair visible, but
it does not push SC075 toward standalone promotion.

### SC076 `OEPrefixIReduction`

SC076 is the clearest case where source support outruns chronology. Campbell and
Fulk both justify a late prefix-vowel reduction note, yet the card has no
positive historical boundary in either direction. That argues strongly for a
singleton scaffold note later, not for folding SC076 into an adjacent production
chapter merely to avoid leaving it alone.

### SC078 `OEWeakTailReduction`

SC078 is the strongest right-edge member. The source tradition clearly supports
late weak-tail reduction, and the card gives it real two-sided chronology: a
broad earlier boundary at `SC070` and a narrower, more interpretable later
boundary at `SC086`. That makes it a plausible future singleton or right-edge
bridge, but one whose exact framing depends partly on how the closing
`SC079-SC087` cluster is later reviewed.

## CAPR formulation versus literature

The literature and the cards do **not** support the current grouped row as one
future production chapter. They support a late region with several internal
seams of different strengths:

1. `SC070-SC071` is a real local early seam.
2. `SC072-SC073` is the strongest middle core.
3. `SC074-SC075` is a narrower local bridge.
4. `SC076` is source-backed but chronology-negative.
5. `SC078` is a historically real right-edge hinge with a forward tie into the
   closing region.

That profile is enough for a later structural split. It is not enough for honest
grouped promotion of `SC069-SC078` unchanged.

## Chronology implications

The chronology cards and the source tradition line up on six practical
conclusions.

1. `SC069` is a broad/far one-sided opener, not a local core.
2. `SC070-SC071` forms the clearest early local pair, but only SC070 has a
   positive boundary on both sides.
3. `SC072-SC073` is the strongest internal production-scale seam in the row.
4. `SC074-SC075` forms a real but narrow local bridge that is probably too small
   and witness-limited to lead the next promotion.
5. `SC076` should not be turned into a positive chronology anchor; its value is
   historical visibility, not tested local chapter structure.
6. `SC078` is the clearest singleton or right-edge bridge candidate, but its
   later `SC086` relation means the closing cluster on the right matters to its
   eventual framing.

## Proposed internal split map

The most plausible later split is not a simple `SC069-SC072` / `SC073-SC076` /
`SC078` division. A more faithful map is:

1. `SC069` as a standalone scaffold opener/context note.
2. `SC070-SC071` as a short early fronting/shortening bridge.
3. `SC072-SC073` as the strongest late-tail shortening/merger core.
4. `SC074-SC075` as a narrow medial-unstressed-`i` lowering bridge.
5. `SC076` as a singleton prefix-reduction note.
6. `SC078` as a singleton weak-tail reduction note or right-edge bridge.

If the project wants fewer units, the most defensible compression is to keep
`SC074-SC075` and `SC076` scaffolded while promoting only `SC072-SC073` or
later `SC078`. What the dossier does **not** support is promoting the present
nine-member row unchanged.

## Ranking of plausible first production targets

1. **`SC072-SC073`** — strongest internal core; both sides historically
   interpretable; best match between source support and local chronology.
2. **`SC078`** — strongest singleton/right-edge candidate; source-backed and
   two-sided, but best judged alongside the closing cluster.
3. **`SC070-SC071`** — real local pair, but less source-backed and less central
   than `SC072-SC073`.
4. **`SC074-SC075`** — tidy but narrow and witness-limited.
5. **`SC069` and `SC076`** — retain as scaffold/context notes unless later
   evidence changes substantially.

## Working recommendation

The row should remain scaffolded in this commit, but it is now
dossier-prepared enough for a real chapter-shape decision.

1. The next move should **not** be grouped promotion of `SC069-SC078`.
2. The likeliest next substantive step is a **structural split** into smaller
   scaffold rows before any production promotion.
3. If the project wants a prose move sooner instead of a structural split, the
   best candidates are `SC072-SC073`, or `SC078` once the closing
   `SC079-SC087` region has also been dossier-reviewed.
4. Further source work is optional rather than blocking. The main remaining issue
   is chapter architecture.

## Open questions

1. Should the first split isolate `SC069` immediately, or is it better kept as
   context for a short `SC070-SC071` bridge?
2. Is `SC072-SC073` strong enough to be promoted before any `SC079-SC087`
   dossier pass, or should the rightward seam be reviewed first?
3. Should `SC074-SC075` remain a pair after a split, or should both simply stay
   scaffolded as supporting material around a promoted `SC072-SC073` core?
4. How explicitly should future prose mention `SC086` for SC078 without making
   the closing cluster sound already resolved?
5. Is there any editorial gain in keeping `SC076` inside a grouped subchapter,
   given that its current card is entirely boundary-limited?

## Dossier status

draft_complete
