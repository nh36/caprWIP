# SC003 West Germanic rhotacism — literature dossier

## Historical phenomenon

This dossier concerns the change of medial `*z` to `*r`, best treated historically as a **post-PWGmc West Germanic rhotacism** and represented in the inventory by `deer`, `hoard`, `learn`, `berry`, and `learn (3sg)`.

## CAPR rule

- change_id: `SC003`
- display_name: `Rhotacism`
- rule_name: `PGmcRhotacism`
- FOMA definition: `*z -> *r || EnglishStarVocalic _ ?`

## Example lexemes

1. `deer`
2. `hoard`
3. `learn`
4. `berry`
5. `learn (3sg)`

## Source support found so far

1. Hogg states that Germanic `/z/` yielded `/r/` in intervocalic position in Old English, though it was generally lost finally [@Hogg1992, p. 37].
2. Ringe and Taylor treat the change as independent in Norse and WGmc, non-uniform within WGmc, and best assigned to the post-PWGmc period [@RingeTaylor2014, pp. 52, 98, 102].
3. Crist distinguishes final `*z` deletion from rhotacism, places the deletion rules first, and argues that the merger cannot be inherited from Proto-Northwest Germanic [@Crist2001, pp. 104-106; @Crist2002, pp. 1, 4].

## Missing source support

1. The historical stage can now be described cautiously as post-PWGmc West Germanic rather than Proto-Germanic proper, but a more fine-grained account of how uniformly the change spread across WGmc daughters would still benefit from fuller source discussion.
2. Additional explicit evidence for exactly how broad the modeled vocalic environment should be would still be useful.

## Chronology/order-test status

1. A validated chronology card now exists from the temporary early-rule first-break harness.
2. The earlier search moved safely across SC002 down to order `2` and then reached the left edge of the tested historical chain with no real break, so the earlier side is boundary-only rather than positive.
3. The later search finds a real historical break at `SC044` OE Breaking: `*líznōjaną` yields `lirnian` instead of `liornian`, with parallel failures in `learn (3sg)`, `learn (iptv.2sg)`, and `meed`.
4. That later boundary is historically interpretable but broad/far rather than tightly local.

## Cautions for eventual reader-facing prose

1. Any later prose should keep rhotacism distinct from final `*z` deletion.
2. Backend and reader-facing prose should use a cautious post-PWGmc West Germanic historical label and should state plainly that `PGmcRhotacism` is only the CAPR rule name.
3. Reader-facing prose should still treat the `SC044` relation as broad/far cross-reference material rather than as a tight adjacency claim.
