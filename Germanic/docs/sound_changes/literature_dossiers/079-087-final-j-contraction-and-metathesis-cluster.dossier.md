# SC079-SC087 Final-j, contraction, and metathesis cluster -- literature dossier

## Rule metadata

- change_ids: SC079; SC080; SC081; SC082; SC083; SC085; SC086; SC087
- display_names: OE J Loss After Heavy; OE Final Geminate Simplification; OE J Strengthening After Front Diphthong; OE Intervocalic J Vocalization; OE Unstressed EI Contraction; OE H Loss; OE Contraction; OE R Metathesis
- FOMA rule names: `OEJLossAfterHeavy`; `OEFinalGeminateSimplification`; `OEJStrengtheningAfterFrontDiphthong`; `OEIntervocalicJVocalization`; `OEUnstressedEIContraction`; `OEHLoss`; `OEContraction`; `OERMetathesis`
- current_orders: 78; 79; 80; 81; 82; 84; 85; 86
- chronology_cards:
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC079-oe-j-loss-after-heavy.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC080-oe-final-geminate-simplification.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC081-oe-j-strengthening-after-front-diphthong.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC082-oe-intervocalic-j-vocalization.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC083-oe-unstressed-ei-contraction.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC085-oe-h-loss.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC086-oe-contraction.md`
  - `Germanic/docs/sound_changes/order_tests/chronology_cards/SC087-oe-r-metathesis.md`
- representative failures / examples from the cards:
  - SC079: `believe`; `bow`; `follow`; `heal`
  - SC080: `lung`
  - SC081: `strew`
  - SC082: `strew`; `bore`; `handle`; `make`
  - SC083: `bore`; `handle`; `learn`; `lick`; `make`
  - SC085: `toe`; `flee`; `slay`; `ten`
  - SC086: `flee`; `slay`; `ten`; `toe`
  - SC087: `burst`
- local chronology summary:
  - `SC079` must follow `SC055` and precede `SC080`; the left side is broad/far, the right side is narrow and local.
  - `SC080` must follow `SC079`; its later side is runner-bounded.
  - `SC081` must follow `SC055` and precede `SC082`; the `SC081 < SC082` relation is the local right edge.
  - `SC082` is the strongest member of the `SC081-SC083` chain: `SC081 < SC082 < SC083`.
  - `SC083` must follow `SC082`; its later side is runner-bounded.
  - `SC085` must follow `SC073` and precede `SC086`, giving the cleanest reciprocal right-side pair in the cluster.
  - `SC086` must follow `SC085`; its later side is runner-bounded, but it is also the target of the already visible `SC078 < SC086` seam.
  - `SC087` has one broad/far earlier boundary at `SC044`; its later side is blocked by the runner limit.

## Historical problem

`SC079-SC087` is the terminal visibility scaffold at the end of the current
sound-change half. It clearly does **not** behave like one eight-change future
production chapter. Instead, the cards and the source tradition point toward
four different kinds of closing material:

1. a final-`j` / final-geminate opening bridge (`SC079-SC080`);
2. a `j`-strengthening / vocalization / unstressed-`ei` chain (`SC081-SC083`);
3. a more standard `h`-loss / contraction core (`SC085-SC086`);
4. an independent terminal `r`-metathesis note (`SC087`).

The closing dossier question is therefore not whether the region matters, but
how it should later be split into smaller scaffold rows before any promotion.

## Source dossier

### Campbell 1959
- source_key: Campbell1959
- locator: §§234--239; §§266--274; §401; §731
- witness_used: `docs/references/campbell_old_english_grammar.txt`
- locator_confidence: section_safe
- terminology: contraction of vowels; vocalization of `g` and `j`; contracted verbs; final geminates
- short quotation:

  > "In early OE a marked tendency is to be observed for g to be vocalized ..."
  >
  > "Most cases are due to the loss of h between diphthong and vowel ..."

- paraphrase: Campbell is strongest for the middle and right side of the cluster. His contraction and vocalization sections directly support the `SC081-SC083` and `SC085-SC086` material through forms such as `stréide`, `céide`, `téon`, `sléan`, `séon`, and `téo` [@Campbell1959, §§234--239, 266--274, 731]. He also notes that final geminates are often graphically shortened, which gives `SC080` at least a handbook backdrop even if not a full standalone chapter [@Campbell1959, §401].
- conditioning: vocalization and contraction arise after loss of `h` or glide material; final geminates simplify in late OE orthographic/phonological practice
- chronology: Campbell supports a late closing zone in which vocalization and contraction cluster together more naturally than the current broad scaffold implies
- examples: `stréide`; `géide`; `téon`; `sléan`; `séon`; `foh`; final geminates
- cautions: Campbell gives weaker direct support for SC079 as a named chapter center and little reason to treat the full eight-change row as one coherent unit

### Hogg 1992
- source_key: Hogg1992
- locator: [metathesis discussion]; [geminate discussion]; [contracted verbs discussion]
- witness_used: `docs/references/hogg_vol1.txt`
- locator_confidence: mixed
- terminology: r-metathesis; degemination of final consonants; contracted verbs
- short quotation:

  > "The final change we have to consider concerns geminate consonants."

- paraphrase: Hogg is valuable because he separates three distinct closing phenomena that the grouped scaffold currently blurs. He treats `r`-metathesis as a real Old English process with broad but variable chronology, notes that final geminates revert to single consonants, and gives a concise account of contracted verbs after the loss of intervocalic `h` [@Hogg1992]. This strongly supports a later split between `SC079-SC080`, `SC085-SC086`, and `SC087`.
- conditioning: metathesis affects `r + short vowel`; final geminates simplify late; contracted verbs arise after intervocalic `h` loss between vowels
- chronology: Hogg implies several different closing episodes rather than one flat terminal row
- examples: `ærn`; `birnan`; `cerse`; `bedd > bed`; `wreon`; `teon`; `flee`; `slay`
- cautions: Hogg gives less direct support for the exact CAPR segmentation of `SC081-SC083`; those members remain more technical than the `SC085-SC086` core or SC087

### Ringe and Taylor 2014
- source_key: RingeTaylor2014
- locator: §§6.9.1--6.9.3; [r-metathesis discussion]
- witness_used: `docs/references/ringe_taylor_linguistic_history_vol2.txt`
- locator_confidence: mixed
- terminology: loss of intervocalic `*h`; contraction; metathesis of `r` with short vowels
- short quotation:

  > "Loss of intervocalic *h and contraction"

- paraphrase: Ringe and Taylor provide the clearest direct historical center for the closing cluster. Their treatment of `h`-loss and contraction supports `SC085-SC086` as a real late Old English subcluster through forms such as `flēon`, `slēan`, `téon`, and `tā` [@RingeTaylor2014, §§6.9.1--6.9.3]. Their later discussion of variable `r`-metathesis confirms that SC087 is historically real, but structurally different from the contraction core [@RingeTaylor2014].
- conditioning: intervocalic `h` is lost; adjacent vowels then contract; metathesis of `r` with short vowels remains variable and dialect-sensitive
- chronology: the strongest closing local pair is `SC085-SC086`, while SC087 belongs to a broader and less tightly local terminal history
- examples: `flēon`; `slēan`; `téon`; `tā`; `beornan`; `berstan`
- cautions: Ringe and Taylor support the `SC085-SC086` center much more strongly than the broader row; the SC078 -> SC086 seam is historically meaningful, but not a reason to merge non-adjacent chapters

### Fulk 2018
- source_key: Fulk2018
- locator: §6.11; §12.21
- witness_used: `docs/references/fulk_comparative_grammar_early_germanic.vision.txt`
- locator_confidence: section_safe
- terminology: loss of `j` between unstressed vowels; contracted verbs
- short quotation:

  > "There was probably loss of j between unstressed vowels ..."

- paraphrase: Fulk is the best source for the technically awkward left and middle closing material. He explicitly argues for loss of `j` between unstressed vowels and then treats contracted verbs in a way that directly supports the `strew`, `beon`, `téon`, `flēon`, and `slēan` pathways [@Fulk2018, §6.11, §12.21]. This makes him especially useful for `SC079`, `SC081-SC083`, and the bridge into `SC085-SC086`.
- conditioning: `j` can be lost between unstressed vowels; contracted verbs arise after loss of `h`, `w`, or `j`
- chronology: Fulk reinforces the idea that the closing row contains more than one real seam, with `SC085-SC086` more book-facing than `SC079-SC083`
- examples: `strēgan`; `beon`; `téon`; `flēon`; `slēan`; `fōn`
- cautions: Fulk makes the technical history real, but he does not turn the current broad grouped scaffold into one obvious chapter

### Luick 1914
- source_key: Luick1914
- locator: §249; [metathesis discussion]
- witness_used: `docs/references/luick_historische_grammatik.txt`
- locator_confidence: mixed
- terminology: `Kontraktionen`; `Metathese`
- short quotation:

  > "Die Kontraktionen, welche durch den Ausfall des h ..."

- paraphrase: Luick gives useful historical depth to the same pattern seen in Campbell and Ringe & Taylor. His contraction discussion explicitly ties later contractions to the loss of `h`, and his metathesis discussion treats the `r` material as a variable later process rather than a neat local pair. That supports the separation of `SC085-SC086` from SC087 in the future split map [@Luick1914, §249].
- conditioning: h-loss creates new hiatus, which is then removed by contraction; metathesis belongs to a separate late consonant/vowel rearrangement history
- chronology: Luick supports two different closing zones rather than one uniform row
- examples: contraction after `h`-loss; `berstan` / `frost` type metathesis
- cautions: Luick is best for framing, not for the exact CAPR segmentation of SC079-SC083

### Sievers-Brunner 1965
- source_key: SieversBrunner1965
- locator: §§127--135; §179
- witness_used: `docs/references/brunner_1965_altenglische_grammatik.txt`
- locator_confidence: section_safe
- terminology: `Hiatus und Kontraktionen`; `Metathese`
- short quotation:

  > "Inlautendes r erfährt oft Metathese ..."

- paraphrase: Sievers-Brunner is especially helpful because it supports both sides of the closing split map. The contraction section gives the older grammar frame for hiatus and contraction, while §179 treats `r`-metathesis explicitly and with the same `berstan`, `frost`, and `cerse` family that reappears in Hogg and Ringe & Taylor [@SieversBrunner1965, §§127--135, §179].
- conditioning: hiatus after consonant loss invites contraction; `r` often moves across a following short vowel in specific late environments
- chronology: the two phenomena are historically real but not identical, which argues against leaving SC085-SC087 in one unsplit production row
- examples: `berstan`; `frost`; `cerse`
- cautions: Sievers-Brunner strengthens the case for a later structural split, not for promoting the current eight-change row unchanged

## Existing project framing

The surrounding project material already narrows the honest reading of this
row.

1. `SC078` is now an explicit singleton scaffold note on the left edge of the
   closing seam. Its later `SC078 < SC086` relation is real, but it must remain
   a cross-reference rather than a license for a non-contiguous `SC078-SC086`
   chapter [@RingeTaylor2014, §§6.9.1--6.9.3].
2. `SC072-SC073` already points forward to `SC085`, which means the closing
   cluster is not isolated from the split late-tail region. But that forward
   relation is again a cross-reference, not chapter architecture.
3. `SC063`, `SC064-SC065`, and `SC066-SC068` already cover the promoted weak-tail
   material on the left. The closing cluster should not be stretched backward
   into that region.

## Member-by-member source notes

### SC079 `OEJLossAfterHeavy`

SC079 is historically plausible but source support is relatively technical. The
best support comes from loss of `j` between unstressed vowels and the broader
contracted-verb tradition. Its card is useful because it gives the left edge of
`SC079-SC080`, but its earlier `SC055` relation is broad/far and does not make
SC079 the natural center of the whole closing row.

### SC080 `OEFinalGeminateSimplification`

SC080 is historically legible and better source-backed than it first looks.
Hogg's late degemination of final consonants and Campbell's note on graphically
shortened final geminates both support it. But the card is one-sided and narrow,
so SC080 reads best as the follower in a short `SC079-SC080` bridge rather than
as a chapter center.

### SC081 `OEJStrengtheningAfterFrontDiphthong`

SC081 is source-backed through Campbell's vocalization material and the `strew`
tradition, but its left boundary is broad/far to SC055 and its local right edge
belongs to SC082. That makes it historically real but subordinate inside a
likely `SC081-SC083` bridge.

### SC082 `OEIntervocalicJVocalization`

SC082 is the strongest member of the `SC081-SC083` chain. It has real local
chronology on both sides and clear lexical support (`strew`, `borian`, `macian`,
etc.). If the project later keeps that chain together, SC082 is the real center.

### SC083 `OEUnstressedEIContraction`

SC083 should stay with the `SC081-SC083` material rather than being merged into
`SC085-SC086`. Its only positive boundary is `SC082 < SC083`, and the source
tradition treats the resulting contracted `-ian` sequences as part of the same
technical closing verbal history rather than as the `h`-loss/contraction core.

### SC085 `OEHLoss`

SC085 is one of the strongest book-facing members of the row. It has real
two-sided chronology (`SC073 < SC085 < SC086`) and is well supported by the
standard contraction tradition. It reads naturally as the left member of the
best closing production-scale pair.

### SC086 `OEContraction`

SC086 is the main center of gravity for the right side of the row. Its card is
one-sided, but it directly reciprocates `SC085` on the left and is also the
landing point of the already explicit `SC078 < SC086` seam. That does not make
SC086 a reason to pull SC078 forward into the same chapter, but it does make
SC086 the clearest closing hinge.

### SC087 `OERMetathesis`

SC087 is a historically strong independent terminal note. The source tradition
for `r`-metathesis is clear, but the card is structurally unlike the others:
its only positive boundary is broad/far back to SC044, and the later side is
blocked by the runner limit. That makes SC087 a good future singleton, not part
of the `SC085-SC086` core.

## CAPR formulation versus literature

The source tradition does **not** support the current grouped row as one future
chapter. It supports at least three different closing architectures:

1. a smaller `SC079-SC080` final-`j` / degemination bridge;
2. a technically coherent `SC081-SC083` vocalization/contraction bridge with
   SC082 as its center;
3. a stronger and more book-facing `SC085-SC086` h-loss/contraction core;
4. an independent terminal `SC087` metathesis note.

That means the current scaffold is primarily a terminal visibility placeholder,
not a promotable production unit.

## Chronology implications

The cards and the source tradition line up on seven practical conclusions.

1. `SC079-SC080` is a real local pair, but SC079's left edge to SC055 is
   broad/far and SC080 is one-sided.
2. `SC081-SC083` forms a real local chain, with SC082 as the strongest internal
   center.
3. `SC083` belongs with `SC081-SC082`, not with `SC085-SC086`.
4. `SC085-SC086` is the strongest compact closing pair in both source support
   and local chronology.
5. `SC086` is the closing cluster's clearest center of gravity because it
   receives both `SC085 < SC086` and the already explicit `SC078 < SC086` seam.
6. `SC087` is historically real but structurally independent and terminal.
7. `SC078 < SC086` should affect the later promotion of SC078 only as a
   cross-reference to the eventual closing subcluster, not as a reason to create
   a non-contiguous SC078-SC086 chapter.

## Proposed internal split map

The most plausible later structural split is:

1. `SC079-SC080` — final-`j` loss and final-geminate simplification bridge
2. `SC081-SC083` — j-strengthening / vocalization / unstressed-`ei` contraction bridge
3. `SC085-SC086` — h-loss and contraction core
4. `SC087` — terminal r-metathesis singleton note

An even narrower option would later split `SC081-SC082` from `SC083`, but the
current dossier does not require that yet. The leading recommendation is the
four-way map above.

## Ranking of plausible first production targets

1. **`SC085-SC086`** — strongest closing subcluster; best match between handbook
   support, local chronology, and the already visible `SC078 < SC086` seam
2. **`SC087`** — strongest independent terminal singleton if the project wants a
   closing note rather than another pair
3. **`SC081-SC083`** — real local bridge with SC082 as center, but more
   technical and less book-facing than `SC085-SC086`
4. **`SC079-SC080`** — useful early closing bridge, but narrower and less
   central than the others
5. **`SC086` alone** — possible only after a split, but weaker than the pair
   `SC085-SC086`

## Working recommendation

The row should remain scaffolded in this commit, but it is now
dossier-prepared enough for a real chapter-shape decision.

1. The next move should **not** be grouped promotion of `SC079-SC087`.
2. The likeliest closing-cluster move is a **structural split** into
   `SC079-SC080`, `SC081-SC083`, `SC085-SC086`, and `SC087`.
3. If the project wants a closing promotion after that split, `SC085-SC086` is
   the strongest first candidate.
4. If the project wants a promotion before more closing-cluster architecture,
   `SC072-SC073` elsewhere in the split late-tail region remains the strongest
   already clarified candidate.
5. SC078 should remain scaffolded until the closing cluster is split or at least
   architecturally judged; the `SC078 < SC086` seam is real, but it should point
   to a future closing subcluster rather than pull SC078 forward into a
   non-contiguous unit.

## Open questions

1. Should `SC081-SC083` stay grouped after a split, or should SC083 later become
   a short right-edge follower note?
2. Is `SC087` better promoted as the first terminal singleton, or only after
   `SC085-SC086` has anchored the closing region?
3. How prominently should a future `SC085-SC086` report mention the already
   explicit `SC078 < SC086` seam without making SC078 sound like part of the
   same chapter?
4. If the project wants the next production move immediately, should it come
   from the closing cluster at all, or should `SC072-SC073` go first while this
   terminal region remains scaffolded?

## Dossier status

draft_complete
