# SC012 Word-internal lþ-voicing — book dossier

## Historical phenomenon

SC012 isolates the development of word-internal `*lþ` to `*ld`.

## Relevant CAPR rule

- `PWGmcLThVoicing`
- `*θ -> *d || *l _`

## Example lexemes

1. `field`
2. `fold`
3. `gold`
4. `wold`

## Source support found so far

1. Ringe and Taylor treat the change as regular in northern WGmc [@RingeTaylor2014, pp. 170--171].
2. Campbell gives a similar West-Germanic-facing formulation with clear lexical examples [@Campbell1959, §414].

## Source gaps

1. The source-stage wording is not perfectly aligned with the inventory's current PWGmc label.
2. The lexical dossier can still be broadened.

## Chronology/order-test status

1. A validated chronology card now exists at `Germanic/docs/sound_changes/order_tests/chronology_cards/SC012-pwgmc-l-th-voicing.md`.
2. The earlier side is boundary-only: the search moved safely across SC011, SC010, SC009, SC008, SC007, SC006, SC005, and SC004 to order `4` and then reached the left edge of the tested expanded-PWGmc chain with no real break.
3. The later side reached order `86` with no real break before the current SC087 boundary.
4. SC012 is now suitable for a cautious reader-facing singleton note, but the stage-label caution and the fully negative chronology must stay explicit.

## Cautions for future reader-facing prose

1. Keep the stage-label caution visible.
2. Do not let the chapter imply that the current inventory label is historiographically settled.
