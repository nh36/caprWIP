# SC079-SC087: Final-j, contraction, and metathesis cluster

## 1. Role in the book

This row is the terminal visibility scaffold of the sound-change half. It
follows the split late-tail region and closes the assembled half with final-`j`,
contraction, and metathesis material. The dossier work now makes one point
clear: the current grouped row is structurally useful, but it is not the right
shape for later production prose.

The internal hierarchy is now clearer than it was in the bare scaffold.
`SC085-SC086` is the strongest closing subcluster. `SC081-SC083` is a real but
more technical local chain with SC082 as the center. `SC079-SC080` is a smaller
opening bridge. `SC087` is a terminal singleton note. The row therefore behaves
less like one chapter and more like a terminal basket of several smaller chapter
types.

## 2. Hard structural constraints

Grouped production reports may cover adjacent changes only. Non-adjacent links
must stay cross-references.

That means:

1. SC078 must **not** be pulled forward into the closing cluster merely because
   `SC078 < SC086` is real.
2. The closing cluster must not be stretched backward into promoted SC063,
   SC064-SC065, SC066-SC068, or the split late-tail units.
3. Any future split must preserve strict chronological order all the way to the
   end of the half.

## 3. Answers to the book-shape questions

### Is there a real local core inside SC079-SC087?

Yes, but again not one single core. The best compact closing core is
`SC085-SC086`. `SC081-SC083` is a separate local chain, `SC079-SC080` is a
smaller opening bridge, and `SC087` behaves as an independent terminal note.

### Should the cluster split into final-j material, contraction material, h-loss/contraction material, and r-metathesis?

Yes. That is now the leading structural reading. The likeliest split is:

1. `SC079-SC080`
2. `SC081-SC083`
3. `SC085-SC086`
4. `SC087`

### Does SC086 form the main center of gravity because of the SC078 -> SC086 relation?

Yes, but only inside the closing cluster. SC086 is the main right-edge hinge
because it closes the local `SC085 < SC086` pair and also receives the
cross-reference from SC078. That is a reason to treat `SC085-SC086` as the
strongest closing subcluster, not a reason to merge SC078 into it.

### Does SC083 belong with contraction, with final-j/e-vowel material, or as a separate bridge?

SC083 belongs with the `SC081-SC083` material, not with `SC085-SC086`. Its only
positive edge is `SC082 < SC083`, and its later side is runner-bounded. It is
best read as the right-edge follower inside the vocalization/contraction chain.

### Is SC087 an independent terminal singleton, or does it belong structurally with a preceding closing subcluster?

SC087 is best read as an independent terminal singleton. It is source-backed and
book-facing, but its card is structurally unlike `SC085-SC086`: the earlier side
is broad/far back to SC044, and the later side is blocked by the runner limit.

### Which member or adjacent pair is the strongest first production target?

Inside the closing cluster, `SC085-SC086` is the strongest first production
target. If the project wants a singleton instead of a pair, SC087 is more
natural than SC086 alone.

### How should the book cross-reference SC078?

Keep SC078 as a right-edge seam note to the left. Production prose for the
closing cluster can mention that SC086 is the target of the SC078 seam, but
SC078 itself must remain in strict order and outside any eventual closing
chapter.

### Should SC078 be promoted only after this cluster is split?

Probably yes. The dossier suggests that SC078 should remain scaffolded until the
closing cluster is structurally split or at least judged, because only then will
it be clear whether the real target of its seam is `SC085-SC086`, SC086 alone,
or a still broader right-edge closing unit.

## 4. Member-by-member book reading

### SC079-SC080

This is the opening bridge of the closing zone. It is historically real, but
narrow. SC079 has the broader leftward tie to SC055, while SC080 is the
one-sided follower that resolves the final-geminate issue.

### SC081-SC083

This is the technical middle bridge. SC082 is the real center, with SC081 to its
left and SC083 to its right. It belongs in the closing architecture, but it is
less obviously the first production target than `SC085-SC086`.

### SC085-SC086

This is the strongest closing core. It has the clearest handbook tradition, the
cleanest local reciprocity, and the best-defined link to the already explicit
SC078 seam.

### SC087

This is the terminal singleton. It is source-backed and narratively attractive,
but its chronology is broad/far and terminal rather than tightly local.

## 5. Proposed split map

The most plausible later split is:

1. `SC079-SC080` — final-`j` loss and final-geminate simplification bridge
2. `SC081-SC083` — j-strengthening / intervocalic-j vocalization / unstressed-`ei` contraction bridge
3. `SC085-SC086` — h-loss and contraction core
4. `SC087` — terminal r-metathesis singleton

This map best fits both the chronology cards and the source hierarchy. A finer
split inside `SC081-SC083` may later be possible, but it is not yet the leading
architectural need.

## 6. Ranking of plausible first production targets

1. **`SC085-SC086`** — strongest closing subcluster and best match for the
   already visible SC078 seam
2. **`SC087`** — strongest closing singleton if the project wants a terminal note
3. **`SC081-SC083`** — technically real and locally coherent, but less book-facing
4. **`SC079-SC080`** — useful opening bridge but not the strongest center

## 7. Promotion readiness judgment

The row is now dossier-prepared enough for human chapter-shape review, but it
is **not** ready for grouped promotion unchanged.

- **Most likely next closing-cluster move:** structural split
- **Strongest later closing promotion target:** `SC085-SC086`
- **Strongest later closing singleton:** SC087
- **Immediate alternative if the project wants prose instead of more structure:** promote `SC072-SC073` first and leave the closing cluster scaffolded

## 8. Editorial status

Prepared for chapter-shape review. No promotion or split is recommended in this
commit. The main live decision is whether the next substantive move should be:

1. split `SC079-SC087` structurally;
2. then promote `SC085-SC086`;
3. or postpone all closing work and promote `SC072-SC073` first while SC078 and
   the closing cluster remain scaffolded.
