# SC007 PWGmc final *ō-lowering before *r — book dossier

## Historical phenomenon

SC007 isolates the lowering of final bimoric `*ō` to `*a` before word-final `*r`.

## Relevant CAPR rule

- `PWGmcFinalOrLowering`
- `*ō -> *a || _ *r .#.`

## Example lexemes

1. `four`
2. `water`

## Source support found so far

1. Ringe and Taylor support the outcome with `four` and `water` [@RingeTaylor2014, pp. 58--59].
2. Fulk gives the same broad historical framing for `ō` before final `r` in WGmc [@Fulk2018, §5.3].

## Source gaps

1. The source discussion is clear but narrow.
2. The current pass did not expand the witness base beyond the two standard examples.

## Chronology/order-test status

1. No validated chronology card exists yet.
2. The ordinary runner can test SC007 directly with `--order-profile expanded-pwgmc`.
3. No real first-break TSV output exists yet.

## Cautions for future reader-facing prose

1. Keep the rule narrow and environment-specific.
2. Do not let the small witness set inflate into a broad long-vowel chapter.
