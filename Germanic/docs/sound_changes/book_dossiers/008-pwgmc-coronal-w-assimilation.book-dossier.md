# SC008 Coronal-w assimilation — book dossier

## Historical phenomenon

SC008 isolates the assimilation of coronal sequences before `*w`, producing `*ww`.

## Relevant CAPR rule

- `PWGmcCoronalWAssimilation`
- `*d -> *w / _ *w`; `*z -> *w / _ *w`

## Example lexemes

1. `four`

## Source support found so far

1. Ringe and Taylor support the change through `four`, `you (dat.pl.)`, and `your (pl.)` [@RingeTaylor2014, pp. 56--57].

## Source gaps

1. The historical evidence is concentrated in a very small set of lexemes.
2. The trace layer is narrower than the source discussion, which also depends on pronominal material.

## Chronology/order-test status

1. No validated chronology card exists yet.
2. The ordinary runner can test SC008 directly with `--order-profile expanded-pwgmc`.
3. No real first-break TSV output exists yet.

## Cautions for future reader-facing prose

1. Keep the evidence set small and explicit.
2. Explain the pronominal support if the rule is ever promoted for readers; do not hide it behind the single `four` trace witness.
