# SC004 EAF ai-monophthongization — book dossier

> **Corrected PROTOFORM pass.** SC004 covers **only** the stressed/root
> `*ái > *ā` development (Chapter 3, Early Anglo-Frisian / North Sea Germanic).
> The unstressed `*ai > *ē` change is separate and earlier — SC014 (see
> `014-015-opening-vowel-prelude.book-dossier.md`). `loam` (`*láimą`) is a
> stressed witness by its PROTOFORM; `whine` carries no `*ai`. Implemented on
> branch `historical-cascade-order` (split commit `f59b758d`, PROTOFORM
> correction `9c71aed3`).

## Historical phenomenon

SC004 isolates the monophthongization of stressed/root `*ái` to `*ā` in the
English line, a North Sea Germanic / Anglo-Frisian areal development. The `*ā` is
later fronted to `ǣ` in the relevant OE environments.

## Relevant CAPR rule

- `EAFAiMonophthongization` (former identifier `PWGmcAiMonophthongization`, retained as a documented alias)
- `{*ái} -> {*ā}` (stressed/root `*ái` only)
- hist_stage `eaf`; hist_scope `north_sea_germanic`; Chapter 3; executable position 25 (after SC028)

## Example lexemes

1. `soul` (SC036 boundary witness)
2. `stone`
3. `bone`
4. `loam` (stressed `*ái` by its PROTOFORM)
5. `one`

24 stressed corpus witnesses (23 attested + `roe` `*ráixōn`, unattested).

## Source support

1. Ringe and Taylor place the `*ai` monophthongization among the post-PNWGmc
   English-line vowel developments [@RingeTaylor2014, pp. 40--41, §6.1.5].
2. Fulk treats `ai`/`au` among the North/West-Germanic shared developments
   [@Fulk2018, §5.2].
3. Campbell describes Anglo-Frisian `*ai > ā` (later fronted) [@Campbell1959,
   §§133--134, §417].
4. Versloot 2017 (verified directly): stressed `*ai` monophthongization as a
   two-wave North Sea Germanic areal diffusion (c. AD 400--900), not a discrete
   Proto-Anglo-Frisian node; supports the stressed side.

## Chronology / order-test status

1. Later boundary `SC036` OE Inter Stress Raising: first-break confirmed with the
   corrected stressed-only rule that `*sáiwalō` yields `sāwel` instead of `sāwol`
   if SC004 is delayed past it (order 33).
2. Earlier side: no corpus break toward the head.

## Cautions for reader-facing prose

1. Present the stressed `*ái > *ā` change only; the unstressed `*ai > *ē` change
   is SC014.
2. Characterise EAF as an operational modelling corridor for a North Sea
   Germanic areal development, not a demonstrated discrete node.
3. `loam` is a stressed witness; `whine` is not an ai-monophthongization case.
