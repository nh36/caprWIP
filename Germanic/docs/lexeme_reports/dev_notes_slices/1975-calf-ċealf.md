---
row_id: 1975
concept: calf
counterpart: ċealf
proto: *kálbaz
protoform: *kálbaz
derivation_class: regular
source_file: Germanic/docs/DEV_NOTES.md
linked_packet_file: Germanic/docs/lexeme_reports/packets/1975-calf-ċealf.md
linked_research_memo_file: Germanic/docs/lexeme_reports/research_memos/1975-calf-ċealf.md
linked_dossier_or_analysis_files:
current_status: current
needs_literature_agent: no
---

# DEV_NOTES material — 1975 calf / ċealf

## Current row state

- CONCEPT: `calf`
- COUNTERPART: `ċealf`
- PROTO: `*kálbaz`
- PROTOFORM: `*kálbaz`
- DERIVATION_CLASS: `regular`
- Live TSV note: `WS palatalized initial (R/T §6.4.1 rule 1: k before front vowel)` [@RingeTaylor2014, §6.4.1]
- Live TSV history: `TSV: cealf → ċealf;`
- `oe_known_problems.tsv`: no entry for `*kálbaz`.
- `report_manifest.tsv`: no manifest entry for row 1975.
- Philological caution carried forward from packet and memo work: repo dictionaries and grammars ordinarily cite plain `cealf`, while the dotted `ċealf` in the TSV is the project's normalized way of making the palatalized initial explicit rather than a claim about manuscript orthography [@ClarkHall1960, s.v. cealf; @BosworthToller1898, s.v. cealf; @RingeTaylor2014, §6.4.1].

## Development-note summary

No securely attachable **row-specific** DEV_NOTES discussion survives here beyond two brief housekeeping fragments. Unlike rows with long debates over paradigm choice or exception handling, row 1975 is not preserved in `DEV_NOTES.md` as a contested philological problem. The live row already treats `*kálbaz -> ċealf` as regular, and the packet trace remains consistent with that decision: final `*-z` is lost, Anglo-Frisian brightening yields `*kælb`, breaking before `*lC` produces `*kealb`, and West Saxon palatalization of initial `k` before the resulting front-vocalic environment gives project-normalized `ċealf` [DEV_NOTES:line-30604-30620; @RingeTaylor2014, §6.4.1].

What DEV_NOTES does preserve is narrower but still useful. First, the row is explicitly inventoried among the `*-aCl-*` / `*-aCr-*` items where **breaking before liquid-plus-consonant is the operative fact**. Second, in the later A-restoration side-effect audit, `*kálbaz` is named again as one of the breaking-conditioned rows for which A-restoration is bled and therefore irrelevant [DEV_NOTES:line-30604-30620; DEV_NOTES:line-36628-36629]. Those notes do not argue for the lexeme, justify the normalization `cealf -> ċealf`, or discuss paradigm history in any detail. Their real value is procedural: they tell later report work not to reopen this row as an A-restoration bug and not to invent a special rescue analysis where the project history preserved none.

The self-sufficient row narrative therefore has to combine the sparse DEV_NOTES material with the stable live-row evidence. Current project policy is: keep `PROTO = PROTOFORM = *kálbaz`; treat the row as a regular citation-form singular derivation; explain `ea` through ordinary breaking before `*lC`; and explain dotted initial `ċ-` as project normalization of the palatalized onset, while acknowledging that handbook and dictionary sources usually print plain `cealf` [DEV_NOTES:line-30604-30620; DEV_NOTES:line-36628-36629; @ClarkHall1960, s.v. cealf; @BosworthToller1898, s.v. cealf; @Fulk2018; @RingeTaylor2014, §6.4.1]. The inherited s-stem background and plural `-r-` forms belong to broader lexeme context, not to a live row-level derivational dispute [@Fulk2018; @RingeTaylor2014].

## Relevant DEV_NOTES fragments

### DEV_NOTES:line-30604-30620

Source heading: inventory of `*-aCl-*` / `*-aCr-*` rows before back-vowel tails  
Source line or section hint: lines 30604-30620  
Status: current  
Issue tags: breaking;a_restoration;row_classification  
Recommended use: cite_in_final_report  
Shared with row IDs:  
Text or paraphrase:
This is the main directly attachable DEV_NOTES fragment for row 1975, but it is an inventory note rather than a developed lexical argument. The scan of OE rows with proto `*aCl` or `*aCr` shapes lists row 1975 as `*kálbaz | ċealf | breaking before *l*+C`. That wording matters because it classifies the row's vowel development as a straightforward breaking case and implicitly excludes it from the A-restoration problem the note is auditing. Later report prose should use this fragment to keep the explanation narrow and accurate: regular breaking before `*lC`, not a special restoration fix, is the DEV_NOTES-level takeaway.

### DEV_NOTES:line-36628-36629

Source heading: predicted side-effects audit for the A-restoration cleanup  
Source line or section hint: lines 36628-36629  
Status: current  
Issue tags: breaking;a_restoration;stability_after_fix  
Recommended use: cite_in_final_report  
Shared with row IDs:  
Text or paraphrase:
This later audit is the clearest surviving current-status statement about row stability. DEV_NOTES says that for breaking-conditioned rows, explicitly including `*kálbaz`, “A-restoration is bled by breaking; unaffected.” That sentence should be preserved almost as-is because it is the project-history warrant for leaving row 1975 alone during the later restoration repair pass. It does not add new philology, but it does document that the row remained regular and unchanged after the broader fix.

## Superseded or diagnostic material

The packet search also surfaced `DEV_NOTES:line-15268-15276`, where a pasted Campbell quotation on `io > eo` includes the phrase “d.s. calf.” That hit is not securely attachable authority for row 1975. It is a passing handbook example inside an unrelated discussion of broken `io/eo`, not a note on the lexeme's citation form, normalization, or derivational status. It should therefore be treated as search noise / diagnostic residue rather than as row evidence.

More broadly, no surviving DEV_NOTES fragment records the project decision to normalize plain `cealf` to dotted `ċealf`; that decision is preserved in the live TSV history, not in a substantive DEV_NOTES argument. Later writers should resist the temptation to back-form a lost debate. For this row the honest slice is a sparse one: DEV_NOTES mostly says “breaking case, unaffected by A-restoration,” and the rest of the working note has to come from the current row plus linked packet/memo research.

## Open questions for later work

- Decide how explicitly the final lexeme report should explain the normalization contrast between project `ċealf` and dictionary/headword `cealf`, since DEV_NOTES itself does not preserve that rationale in prose.
- Decide whether the final report should mention inherited plural/background forms such as `cealfru` or keep the report tightly focused on the singular citation-form derivation `*kálbaz -> ċealf` [@ClarkHall1960, s.v. cealf; @BosworthToller1898, s.v. cealf; @Fulk2018].
- If a future audit wants a fuller project-history citation for the `cealf -> ċealf` change, look outside DEV_NOTES; the surviving DEV_NOTES material for row 1975 is classificatory rather than argumentative.
