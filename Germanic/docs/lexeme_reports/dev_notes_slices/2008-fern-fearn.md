---
row_id: 2008
concept: fern
counterpart: fearn
proto: *fárnaz
protoform: *fárnaz
derivation_class: regular
source_file: Germanic/docs/DEV_NOTES.md
linked_packet_file: Germanic/docs/lexeme_reports/packets/2008-fern-fearn.md
linked_research_memo_file: Germanic/docs/lexeme_reports/research_memos/2008-fern-fearn.md
linked_dossier_or_analysis_files: Germanic/docs/analysis/arestoration_r_l_research.md
current_status: current
needs_literature_agent: no
---

# DEV_NOTES material — 2008 fern / fearn

## Current row state

- CONCEPT: `fern`
- COUNTERPART: `fearn`
- PROTO: `*fárnaz`
- PROTOFORM: `*fárnaz`
- DERIVATION_CLASS: `regular`
- Live TSV note: `Proto: oblique *farnăn→*farnăz (m. a-stem nom.sg.; Kroonen)`
- `oe_known_problems.tsv`: no row-local entry for `2008`, `*fárnaz`, `*farnă-`, or OE `fearn`.
- `report_manifest.tsv`: no manifest entry for row 2008.
- Packet / memo state: the packet's compact trace already yields `*fárnaz > *fárna > *fárn > *færn > fearn`, and the research memo treats the row as a regular breaking case whose only live caution is explanatory hygiene around the comparative stem/gender note rather than any OE-output failure [@Campbell1959, §§216-219; @Kroonen2013; @Orel2003].

## Development-note summary

No rich row-specific DEV_NOTES dossier survives for row 2008. The only securely attachable DEV_NOTES authority is the late audit of OE rows with proto `*-aCl-*` or `*-aCr-*` before a back-vowel tail, where the row is listed tersely but unambiguously as `| 2008 | *fárnaz | fearn | breaking |` [DEV_NOTES:line-30604-30620]. In that context the note is not trying to solve a fern-specific problem; it is checking whether an A-restoration fix for a different lexeme would spill over onto other `a + liquid/consonant` rows, and it treats `*fárnaz → fearn` as an ordinary control case rather than as an exception or repair target [DEV_NOTES:line-30604-30620].

That brief DEV_NOTES classification matches the current derivational state. Nothing in the packet, memo, or live TSV suggests that row 2008 needs a paradigm-cell rescue, a derivation-class change, or a special OE exception label. The phonological story is the standard one for inherited `a` before `rC`: the project input `*fárnaz` loses final `-z`, then final `-a`, undergoes Anglo-Frisian brightening to `*færn`, and then shows OE breaking to `fearn` before `r` plus consonant [DEV_NOTES:line-30604-30620; @Campbell1959, §§216-219; @Hogg1992; @RingeTaylor2014]. For normal workflow, the row should therefore be read as a regular breaking item whose DEV_NOTES footprint is sparse because the row never became a problem case.

The only point that still deserves explicit working-note attention is philological labeling, not phonological derivation. Comparative sources do not cite the proto lexeme in exactly the same way: Kroonen gives stem-style `*farna- m.` and cites OE `fearn, fern`, while Orel gives `*farnan` as a neuter noun; repo-local lexical sources still support keeping the OE target as attested `fearn` [@Kroonen2013; @Orel2003; @ClarkHall1960, s.v. "fearn"; @BosworthToller1898, s.v. "fearn"]. The live row's nominative-style `*fárnaz` is therefore best understood as the project's FST input form, not as a claim that the comparative dictionaries are in perfect agreement. Because DEV_NOTES never built a row-specific dispute around that stem/gender variation, this slice should say so plainly: there is no hidden fern-specific DEV_NOTES controversy to recover beyond the short inventory note and the regular derivation it presupposes.

## Relevant DEV_NOTES fragments

### DEV_NOTES:line-30604-30620

- Source heading: `Inventory of OE rows with proto *-aCl-* or *-aCr-* before a back-vowel tail`
- Source line or section hint: `lines 30604-30620`
- Fragment type: `phenomenon_context_for_lexeme`
- Status: `current`
- Issue tags: `breaking`; `regular_row`; `a_restoration_scope`; `protoform_note`
- Recommended next use: `cite_in_final_report`
- Shared with row IDs:

This is the only securely attachable DEV_NOTES fragment for row 2008, and it is valuable precisely because it is so matter-of-fact. In the A-restoration spillover audit, DEV_NOTES records the row as `| 2008 | *fárnaz | fearn | breaking |` [DEV_NOTES:line-30616]. That wording shows that the project did **not** treat fern as an A-restoration casualty, a hidden analogical form, or a mismatch needing repair. Instead, the row functioned as one of the `a + liquid/consonant` controls whose `ea` is already explained by ordinary OE breaking before `rC` [DEV_NOTES:line-30604-30620; @Campbell1959, §§216-219; @Hogg1992; @RingeTaylor2014]. Later report prose can safely cite this fragment to show the project's settled internal classification, but it should not oversell the fragment as if it preserved a richer fern-specific debate than it actually does.

## Superseded or diagnostic material

There is no superseded row-specific DEV_NOTES analysis for row 2008. The closest thing to secondary project history is the diagnostic list in `Germanic/docs/analysis/arestoration_r_l_research.md`, which also names row 2008 as a breaking-before-`*rC` case. That file is useful as confirmation that earlier A-restoration auditing treated `fearn` as a control item, but it is not stronger authority than the live row and should not be mistaken for an independent lexical argument.

The comparative stem/gender disagreement likewise belongs here as diagnostic background rather than as superseded row policy. Kroonen's `*farna- m.` and Orel's `*farnan` do not force a change to the current modeled input `*fárnaz`; they simply explain why the TSV note tries to preserve a stem/paradigm observation alongside an already-regular derivation [@Kroonen2013; @Orel2003]. The row's current OE target `fearn` remains the best-supported citation form in repo-local lexical sources, while Kroonen's additional OE variant `fern` should stay a comparative note unless later manuscript-specific work makes it operationally relevant [@ClarkHall1960, s.v. "fearn"; @BosworthToller1898, s.v. "fearn"; @Kroonen2013].

## Open questions for later work

- If the eventual lexeme report or TSV note is rewritten, make the three-way distinction explicit: comparative proto headword (`*farna-` / `*farnan` in dictionaries), project FST input (`*fárnaz`), and attested OE target (`fearn`).
- Decide whether the final report should mention Kroonen's OE variant `fern` in a short philological note or omit it unless manuscript- or dialect-specific evidence is added elsewhere.
- If later slice/index work prefers a richer shared background note on OE breaking before `rC`, decide whether row 2008 should receive a copied shared fragment in addition to the present short inventory fragment; at present no richer row-specific DEV_NOTES authority survives.
