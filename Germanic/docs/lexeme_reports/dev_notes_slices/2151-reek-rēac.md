---
row_id: 2151
concept: reek
counterpart: rēac
proto: *ráukiz
protoform: *ráukaz
derivation_class: reconstructed_oe
source_file: Germanic/docs/DEV_NOTES.md
linked_packet_file: Germanic/docs/lexeme_reports/packets/2151-reek-rēac.md
linked_research_memo_file: Germanic/docs/lexeme_reports/research_memos/2151-reek-rēac.md
linked_dossier_or_analysis_files:
  - Germanic/docs/dossier-reek-2026.md
current_status: current
needs_literature_agent: no
---

# DEV_NOTES material — 2151 reek / rēac

## Current row state

- CONCEPT: `reek`
- COUNTERPART: `rēac`
- PROTO: `*ráukiz`
- PROTOFORM: `*ráukaz`
- DERIVATION_CLASS: `reconstructed_oe`
- Live TSV note (abridged): the row was retargeted on 2026-04-30 from attested `rēc` to reconstructed West Saxon `rēac`; the cascade already derives `rēac` from `*ráukaz`, while modelling attested `rēc` would require an Anglian smoothing rule that would regress ordinary West-Saxon `ēa` outcomes.

## Development-note summary

The controlling note for the live row is the late project-status update at DEV_NOTES:line-1435-1439. That note explicitly says the earlier `*ráukaz → rēac` mismatch against attested `rēc` was retired when the cogset was retargeted to reconstructed West Saxon `*rēac`, and that the row is now a regular match in `reconstructed_oe`. The current slice therefore has to keep three things separate: the cognate-set etymon `*ráukiz`, the OE modelling input `*ráukaz`, and the row target `rēac` as a reconstructed citation form rather than a directly attested manuscript lemma.

The older reek dossier remains necessary because it preserves the comparative baseline that the retargeting note does not repeat. Kroonen's entry is quoted there as `"*rauki- m. 'smoke' — ... OE réc m. 'id.' ... An i-stem derived from the strong verb *reukan- ~ *rūkan-"` [@Kroonen2013, p. 407]. In other words, the inherited comparator for the family is still i-stem `*ráukiz`, and the directly attested Old English form carried in the comparative dictionaries is `rēc`, not `rēac`. The same note also spells out the regular dialect split if one insists on that i-stem target: Campbell writes that `"The i-umlaut of ēa in West Saxon is īe"` [@Campbell1959, §201], while Ringe and Taylor state that long `*éa` was umlauted to `ie` in West Saxon but to `é` in the other dialects [@RingeTaylor2014, §6.6.3]. That is the superseded comparator behind the old mismatch: regular i-stem development points toward WS `*rīeċ` or Anglian `rēc`, not toward the live reconstructed target `rēac`.

The long philological deep-dive is still worth preserving because it records what the handbook searches did and did not find. It quotes Campbell's `§200(5)` on regular `*au`-umlaut outcomes and Ringe-Taylor on the same dialect split, then notes that Campbell, Ringe-Taylor, and Hogg do not single out `rēc` as a named irregularity, even though Kroonen still cites OE `réc` under `*rauki-` [@Campbell1959, §200(5); @RingeTaylor2014, §6.6.3; @Hogg1992, §§5.79--5.83; @Kroonen2013, p. 407]. That fragment is not current row policy by itself, but it remains the best replacement-note record of the philological problem: attested `rēc` is real, handbook silence is real, and the project had to decide whether to bend the row toward attestation or toward the regular cascade.

The `*ráukaz` track enters because the FST probe showed that a no-i-trigger a-stem input already gives the reconstructed form now used by the row. DEV_NOTES records the probe verbatim: `ráukaz    rēac`, followed by the conclusion that the system produces `rēac` from `*ráukaz` because `*au` develops to `ēa` and no i-umlaut applies. Campbell's smoothing discussion is then invoked only as a follow-up question about whether `rēac` could later become `rēc` [@Campbell1959, §§279--282]. For current row purposes, the important point is narrower: `*ráukaz → rēac` is the regular comparator that now underwrites the reconstructed target.

The slice also has to preserve the abandoned detour through `*rōkiz`. The deep-dive temporarily recommended changing the row to `*rōkiz` because the FST gave `rēċ`, which looked like a neat way to reach attested `rēc` on the page. That recommendation is now explicitly dead. The later closure note says the `*rōkiz` recommendation is `withdrawn`, and it treats any proto input that yields palatal `ċ` as philologically wrong because Modern English `reek` /riːk/ and the related weak verb evidence require OE velar /k/, not palatal /tʃ/ [@ClarkHall1960, p. 255; @BosworthToller1898, s.v. `réc`]. Later report writers should therefore treat `*rōkiz` only as a superseded project workaround that solved the wrong problem.

The closure note is itself mixed-status evidence and must be read carefully. Its known-problems triage is superseded by the later retargeting note, and its proposal to switch the row's `PROTO` as well as `PROTOFORM` to `*ráukaz` no longer matches the live TSV, which now keeps `PROTO = *ráukiz` while using `PROTOFORM = *ráukaz`. But the closure still preserves two current reasons that survived the retarget: first, adding a general smoothing rule to force attested `rēc` would damage ordinary West-Saxon `ēa` words; second, the project decided that attested `rēc` should be explained in note/report prose rather than forced through the live cascade. That is why the row now stands as reconstructed `rēac`, while attested `rēc` remains the explicit philological background and the main superseded target.

## Relevant DEV_NOTES fragments

### DEV_NOTES:line-1435-1439

- Source heading: `Project Status (as of 2026-04-30) — research phase complete`
- Source line or section hint: `lines 1435-1439`
- Status: `current`
- Issue tags: `row_policy`; `reconstructed_oe`; `retargeting`
- Recommended use: `cite_in_final_report`
- Shared with row IDs:

This short status note is the governing current-policy fragment. It says directly that the earlier mismatch pairing `*ráukaz → rēac` against attested `rēc` has been retired because the cogset was retargeted to reconstructed West Saxon `*rēac`, which the FST already produces by regular development. It also records the row-class consequence in plain terms: the row is now a match under `reconstructed_oe`. Any later report should cite this fragment first when explaining why the live row no longer targets the attested dictionary headword.

### DEV_NOTES:line-34241-34253

- Source heading: `Lexeme overview and cognates — Proto-Germanic reconstruction`
- Source line or section hint: `lines 34241-34253`
- Status: `current`
- Issue tags: `proto_vs_protoform`; `comparative_background`; `cognate_set`
- Recommended use: `cite_in_final_report`
- Shared with row IDs:

This fragment preserves the comparative starting point that still matters even after retargeting. DEV_NOTES quotes Kroonen's entry as `"*rauki- m. 'smoke' — ... OE réc m. 'id.' ... An i-stem derived from the strong verb *reukan- ~ *rūkan-"` [@Kroonen2013, p. 407]. The current row does **not** use that form as its direct OE input, but the fragment remains current because it explains why the live TSV keeps `PROTO = *ráukiz` for the cognate set while separating out `PROTOFORM = *ráukaz` for the OE modelling path.

### DEV_NOTES:line-34305-34360

- Source heading: `Vowel discrepancy: īe (WS) vs. ē (Anglian) as i-umlaut of *au`
- Source line or section hint: `lines 34305-34360`
- Status: `superseded`
- Issue tags: `regular_comparator`; `i_umlaut`; `dialect_split`; `old_target`
- Recommended use: `use_to_explain_superseded_analysis`
- Shared with row IDs:

This is the clearest old note on what the regular i-stem comparator would have been if the row had continued to target attested `rēc`. It preserves Campbell's statement that `"The i-umlaut of ēa in West Saxon is īe"` and the Anglian contrast `ēa + i → ē` [@Campbell1959, §§193--201], then points out that the default OE cascade models West-Saxon `ēa → īe`, not Anglian monophthongization. The fragment is superseded as row policy because the live row no longer asks the FST to deliver attested `rēc`, but it should be kept because it states the regular comparator explicitly: inherited i-stem `*ráukiz` predicts WS `*rīeċ` or Anglian `rēc`, whereas current row-target `rēac` depends on the later a-stem retargeting.

### DEV_NOTES:line-35039-35137

- Source heading: `What handbook authors actually say about rēc`
- Source line or section hint: `lines 35039-35137`
- Status: `current`
- Issue tags: `literature_survey`; `attested_outcome`; `handbook_silence`
- Recommended use: `cite_in_final_report`
- Shared with row IDs:

This fragment is still the best compact record of the philological literature check. It quotes Campbell's `§200(5)` on regular umlaut of OE `ēa` from PGmc `au`, Ringe-Taylor on the dialect split `WS ie` versus non-WS `ē`, and Kroonen on OE `réc`, then states the negative result explicitly: Campbell, Ringe-Taylor, and Hogg do **not** name `rēc` as a recognized exception [@Campbell1959, §200(5); @RingeTaylor2014, §6.6.3; @Hogg1992, §§5.79--5.83; @Kroonen2013, p. 407]. For later work this matters less as a solution than as a guardrail: the project's reconstruction/retargeting choice was made in the absence of a handbook-sanctioned one-line fix.

### DEV_NOTES:line-35561-35587

- Source heading: `FST test: *ráukaz → rēac`
- Source line or section hint: `lines 35561-35587`
- Status: `current`
- Issue tags: `protoform_vs_proto`; `fst_probe`; `regular_reconstructed_target`
- Recommended use: `cite_in_final_report`
- Shared with row IDs:

This fragment contains the direct probe that now supports the live row. DEV_NOTES gives the output exactly as `ráukaz    rēac`, then glosses the path as `*au → *éa`, no i-umlaut because there is no i-trigger in a-stem `-az`, and final loss to `rēac`. The note was originally framed against attested `rēc`, but its positive content is now current: the existing cascade already derives reconstructed `rēac` from `*ráukaz` without any special repair rule. The smoothing discussion attached to the fragment should be read only as background on the abandoned attested-`rēc` target [@Campbell1959, §§279--282].

### DEV_NOTES:line-35603-35775

- Source heading: `Revised proto-form recommendation: *raukiz, *raukaz, or *rōkiz?`
- Source line or section hint: `lines 35603-35775`
- Status: `superseded`
- Issue tags: `superseded_workaround`; `palatalization`; `project_history`
- Recommended use: `use_to_explain_superseded_analysis`
- Shared with row IDs:

This fragment records the strongest abandoned workaround and should remain visible so it does not get rediscovered as if new. The note temporarily argued that `*rōkiz` was the best row-level input because the FST produced `rēċ`, which seemed to regularize attested `rēc` while preserving the wider comparative picture only by positing a WGmc-specific reshaping. That recommendation is no longer current. It is important mainly because later closure notes explicitly withdraw it after deciding that `rēċ` solves the orthographic shape at the cost of the required velar consonant.

### DEV_NOTES:line-37928-37981

- Source heading: `*rēc closure (row 859/ID 2151): triaged as intractable (Anglian smoothing relic)`
- Source line or section hint: `lines 37928-37981`
- Status: `superseded`
- Issue tags: `closure_note`; `smoothing`; `velar_requirement`; `project_history`
- Recommended use: `use_to_explain_superseded_analysis`
- Shared with row IDs:

This closure note is superseded in its row-state outcome but still essential as chronology. It withdraws the earlier `*rōkiz` recommendation, insists that anything yielding palatal `ċ` is wrong because Modern English `reek` and OE verbal evidence point to velar /k/, and explains why a general smoothing rule for attested `rēc` was rejected as too damaging for ordinary West-Saxon `ēa` lexemes. What is no longer current is the ledger treatment and the statement that both `PROTO` and `PROTOFORM` should be switched to `*ráukaz`; the live row now keeps cognate-set `PROTO = *ráukiz` and resolves the modelling problem differently by retargeting the row to reconstructed `rēac`. Use this fragment for project chronology and for the surviving anti-`*rōkiz` / anti-general-smoothing rationale, not as the current row state.

## Superseded or diagnostic material

Two superseded phases need to remain explicit. First, the i-stem mismatch analysis around attested `rēc` is superseded as live row policy, but it still supplies the regular comparator (`*rīeċ` vs. `rēc`) and the handbook-silence audit. Second, the later `*rōkiz` and known-problems closure phase is also superseded as row policy, but it preserves the reasons the project now refuses both palatal `rēċ` and a general smoothing fix. The current row depends on both pieces of history even though it adopts neither old target.

## Open questions for later work

- Decide whether the final lexeme report should cite the project-status retargeting note together with the `*ráukaz → rēac` probe in one paragraph or keep policy and derivation separate.
- Decide how much of the handbook-silence survey belongs in a final report, as opposed to a shorter note that simply states that no standard handbook appears to single out `rēc`.
- If attested `rēc` is discussed in the final report, keep the distinction explicit between current row target `rēac`, attested outcome `rēc`, and superseded workaround `*rōkiz → rēċ`.
- If the final report cites the closure note, state plainly that its known-problems triage and full `PROTO` switch are historical rather than live row policy.
