# Research memo — 2136 needle / nǣdl

## Starting point

- **ID:** 2136
- **CONCEPT:** needle
- **COUNTERPART:** `nǣdl`
- **PROTO:** `*nḗθlō`
- **PROTOFORM:** `*nḗðlō`
- **DERIVATION_CLASS:** `early_analogy`
- **NOTE:** `R/T p.435: PGmc *nēdlō has Verner's alternation; OE nǣdl reflects *d variant`

The live row already derives correctly to `nǣdl`. The memo problem is therefore not a live mismatch but the need to explain the split between cognate-set `PROTO`, OE-facing `PROTOFORM`, and the attested OE target, while separating current evidence from older mismatch history.

## Packet evidence assessment

- **Authoritative/current:** the live TSV row; the packet's compact derivation trace showing `*nḗðlō -> nǣdl`; and the packet's `DEV_NOTES.md` excerpt at `1343-1356`, which still states the core project position that OE `nǣdl` reflects the voiced Verner-grade and is not explained by the ordinary `*lþ > ld` rule.
- **Useful background:** the packet's lexical-table hit in `old_english_wiktionary.tsv`; the packet's Brunner/Sievers excerpt on `nǣdl ~ nēdl`; and the packet's preservation of the older row-specific analysis around `10243-10253`, which is useful project chronology once treated as historical rather than live authority.
- **Stale or superseded:** the packet's `DEV_NOTES.md` material at `10243-10253` and `10257-10259`, because those lines belong to the stage when row 2136 still needed its protoform fix; the mismatch progress log entry at `10391`; and any packet reuse of the old `*nēθlō -> expected nǣdl` framing as if it still described the live row.
- **Irrelevant or misleading:** the lack of a manifest entry does not mean the lexeme is trivial; and autogenerated debug-snapshot trace text is only diagnostic output, not a manual pilot/full report.

## Additional repo research

Checked beyond the packet:

- `Germanic/docs/DEV_NOTES.md` (`1343-1358`, `10243-10285`, `29893-30012`, `31229-31237`)
- `Germanic/docs/germanic_notes/final_vowel_apocope_investigation.md`
- `Germanic/data/oe_known_problems.tsv` (no entry for this lexeme)
- `Germanic/data/old_english_wiktionary.tsv`
- `Germanic/docs/lexeme_reports/coverage_audit.md` and `Germanic/docs/lexeme_reports/pilot/` (no pilot/full report for this lexeme found)
- `docs/references/ringe_taylor_linguistic_history_vol2.txt`
- `docs/references/ringe_vol1_pie_to_pgmc.txt`
- `docs/references/campbell_old_english_grammar.txt`
- `docs/references/hogg_vol1.txt`
- `docs/references/clark_hall_concise_anglo_saxon_dictionary.vision.txt`
- `docs/references/kroonen_etymological_dictionary_pgmc.vision.txt`
- `docs/references/legacy/orel_handbook_germanic_etymology.txt`

No full dossier or analysis file specific to this lexeme was named in the packet or TSV note. The extra repo pass nevertheless matters because it shows three distinct layers of evidence:

1. `Ringe & Taylor` and `Ringe vol. 1` support a real alternation `*nēþlō- ~ *nēdlō-` / `*nēþlō- ~ *nēdlō-`, matching the row note's appeal to Verner alternation [@RingeTaylor2014; @Ringe2006].
2. `Campbell`, `Hogg`, `Clark Hall`, and the cluster-class material in `DEV_NOTES` support `nǣdl` as an attested OE lexeme with an unbroken `-dl` cluster, not as a special paradigm-cell workaround [@Campbell1959; @Hogg1992; @ClarkHall1960].
3. Older repo diagnostics still preserve the pre-fix state where the row used the voiceless protoform and therefore mismatched. That history is useful, but it is no longer the live row design.

Kroonen and Orel are background rather than controlling project authority here. Kroonen's deeper etymological entry starts from a different comparative reconstruction (`*nēplō-`) and lists both OE `næþl` and `nædl`; Orel gives a simpler `*nèþlò`. Those entries are valuable comparative background, but the live row follows the repo's Ringe/Taylor-style voiced/voiceless alternant distinction rather than adopting Kroonen's headword directly [@Kroonen2013; @Orel2003].

## Reconstruction and early-stage forms

This row needs a strict three-way distinction.

1. **Cognate-set proto / etymological headword:** `PROTO = *nḗθlō` is the comparative headword carried by the row.
2. **Project input form for derivation:** `PROTOFORM = *nḗðlō` is the voiced Verner-grade alternant actually fed to the OE cascade.
3. **OE target form:** `nǣdl` is the attested OE citation form represented by the row.

The packet trace shows that once the project input is the voiced alternant, the rest of the derivation is regular on project terms: `*nḗðlō -> *nḗdlō -> *nḗdlu -> *nǣdlu -> *nǣdl -> nǣdl`. The real issue is therefore upstream alternant selection, not OE-side repair.

That is why the older repo line "change protoform to `*nēdlō` for the OE row" is now stale as live instruction: the row has already implemented the relevant fix in project notation by separating voiceless `PROTO` from voiced `PROTOFORM`. The memo should not collapse those into a single undifferentiated "PGmc `*nēdlō`" statement.

## Old English philology

`nǣdl` should be treated as an attested OE headword, not a reconstructed form and not a paradigm-cell substitute. `old_english_wiktionary.tsv` gives `needle = nǣdl`, `Clark Hall` lists `nædl (ē) f.`, and the cluster-class comparison in `DEV_NOTES` explicitly counts `nǣdl` among the directly attested unbroken `-Cl/-Cn/-Cm#` nouns.

The philological subtlety is not whether OE had `nǣdl`, but how to describe its consonant history and orthographic presentation:

- `Campbell` lists `nédl` among the expected unbroken forms after `t` and `d`, so the OE cluster itself is not problematic [@Campbell1959].
- `Hogg` likewise treats forms like `bosm`, `hræfn`, `spatl`, and `nidl/nǣdl` as possible syllabic-sonorant outcomes, sometimes with competing epenthetic-vowel spellings elsewhere in the class [@Hogg1992].
- The Brunner/Sievers quotation reused in `DEV_NOTES` specifically notes `nǣdl ~ nēdl`, which is useful as orthographic background but does not justify changing the row away from the attested citation form `nǣdl`.

So the safe philological claim is straightforward: the row targets attested OE `nǣdl`, an unbroken citation form; the special explanatory burden lies in the inherited voiced/voiceless alternant, not in OE attestation or paradigm-cell choice.

## Project problem and solution

The project problem was originally that the row's note already acknowledged a voiced Verner-grade source, while the older OE-facing protoform still used the voiceless consonant and therefore mismatched. The current project solution is the right kind of solution:

- keep `PROTO = *nḗθlō` as the cognate-set headword;
- keep `PROTOFORM = *nḗðlō` as the OE-facing derivational input;
- keep `COUNTERPART = nǣdl` as the attested OE target.

On that reading, `DERIVATION_CLASS = early_analogy` remains defensible. The special move is upstream selection of the voiced alternant for the OE derivation, not a late OE paradigm-cell rescue. This row is therefore closer to other early-stage stem/alternant-selection cases than to the `late_analogy` paradigm-cell rows.

## Paradigm probe

A paradigm probe is **not required**.

This row does not depend on choosing among competing OE inflectional cells. The decisive contrast is between the comparative/cognate-set `PROTO` and the voiced OE-facing `PROTOFORM`, and the live derivation trace already confirms that the selected input yields the citation form `nǣdl`. If the supervisor later wants a tiny audit table, a simple nom.sg.-style comparator of `*nḗθlō` versus `*nḗðlō` would be enough; there is no missing set of OE paradigm cells that blocks a final report.

## Recommended final report

Recommend a short final report saying that row 2136 targets attested OE `nǣdl`, that the row must distinguish cognate-set `PROTO = *nḗθlō` from OE-facing `PROTOFORM = *nḗðlō`, and that the note-bearing issue is inherited Verner alternation rather than a live OE mismatch or paradigm-cell substitution.

## Data-change recommendations

- **TSV `PROTO`:** no change recommended. `*nḗθlō` can stay as the cognate-set / etymological headword.
- **TSV `PROTOFORM`:** no change recommended. `*nḗðlō` is the correct OE-facing input.
- **TSV `COUNTERPART`:** no change recommended. `nǣdl` is the right attested target.
- **TSV `DERIVATION_CLASS`:** no change recommended. `early_analogy` is still a workable label because the special move is early alternant selection, not a late inflectional-cell workaround.
- **TSV `NOTE`:** change recommended. It should explicitly distinguish `PROTO` from `PROTOFORM`, e.g. by saying that the row keeps voiceless comparative `*nēþlō`/`*nḗθlō` as the headword but derives OE from the voiced Verner-grade `*nēðlō`/`*nḗðlō`.
- **`oe_known_problems.tsv`:** no change recommended.
- **`DEV_NOTES` / dossier text:** change recommended for `DEV_NOTES`; no dossier change identified. The section at `10243-10259` should be marked more clearly as implemented historical workflow rather than a live to-do, and the older diagnostic wording in `final_vowel_apocope_investigation.md` should not be reused as current row authority without annotation.
