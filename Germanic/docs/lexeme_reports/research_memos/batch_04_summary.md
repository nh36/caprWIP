# Batch 04 summary

## Rows memoed

1. **2162 rust / rust** — **Lane 1** (`unexplained_unmodelled`)
2. **2151 reek / rēac** — **Lane 2** (`reconstructed_oe`)
3. **2011 find / fundene** — **Lane 3** (`late_analogy`)
4. **1951 birth / byrd** — **Lane 4** (`regular` with non-empty `NOTE`)

## Ready for final report

- **Ready:** 2151 reek / rēac; 1951 birth / byrd
- **Not yet ready:** 2162 rust / rust; 2011 find / fundene

## Rows needing paradigm probes

- **Needs paradigm probe:** 2162 rust / rust; 2011 find / fundene
  - **2162** needs a real contrastive probe across multiple stem/case candidates.
  - **2011** still lacks a built-in probe spec, even though the memo records a manually supported winning cell.
- **No paradigm probe needed:** 2151 reek / rēac; 1951 birth / byrd

## Rows recommending TSV changes

- **2162 rust / rust** — TSV `PROTO`, TSV `PROTOFORM`, TSV `NOTE`
- **2151 reek / rēac** — TSV `NOTE`
- **1951 birth / byrd** — TSV `NOTE`

## Rows recommending DEV_NOTES or dossier cleanup

- **2162 rust / rust** — `DEV_NOTES` cleanup and `oe_known_problems.tsv` reference cleanup
- **2151 reek / rēac** — stale wording / cross-reference cleanup in TSV note and ideally `DEV_NOTES`
- **2011 find / fundene** — optional cleanup to mark older `funden`-as-regular history more explicitly as superseded
- **1951 birth / byrd** — no cleanup required beyond the note adjustment

## Lane usage

- No lane was exhausted or skipped.
- No refill rule was needed.
- The round again followed the preferred one-per-lane layout exactly.

## Systematic workflow issues noticed

- **Probe infrastructure is lagging behind memo conclusions.** `find` and `rust` both show that the memo stage can identify the decisive paradigm cells before a reusable built-in probe spec exists.
- **Some remaining “exception” rows are actually metadata-correction candidates.** `rust` is the clearest example in this round: the row should stay exceptional, but its inherited-case analysis is not the right one.
- **Normalized project targets still need better philology notes.** `reek` and `birth` both benefited from making the distinction between project-normalized target and dictionary/headword evidence explicit.

Batch 04 is complete. The rollout continues to the next four-row round.
