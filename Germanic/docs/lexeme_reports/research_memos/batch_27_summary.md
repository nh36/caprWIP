# Batch 27 summary

## Rows memoed

1. **2179 sheep / sċēap** — **Lane 4** (`regular`)
2. **2181 shilling / sċilling** — **Lane 4** (`regular`)
3. **2186 show / sċēawian** — **Lane 4** (`regular`)
4. **2189 sieve / sife** — **Lane 4** (`early_analogy`)

## Ready for final report

- **Ready:** 2179 sheep / sċēap; 2181 shilling / sċilling; 2186 show / sċēawian
- **Not ready:** 2189 sieve / sife

## Rows needing paradigm probes

- **Needs paradigm probe:** none
- **No paradigm probe needed:** 2179 sheep / sċēap; 2181 shilling / sċilling; 2186 show / sċēawian; 2189 sieve / sife

## Rows recommending TSV changes

- **2179 sheep / sċēap** — TSV `NOTE`
- **2181 shilling / sċilling** — light TSV `NOTE` clarification
- **2189 sieve / sife** — TSV `PROTO` and TSV `NOTE`

## Rows recommending DEV_NOTES or dossier cleanup

- **2181 shilling / sċilling** — minor `DEV_NOTES` heading cleanup so stale `*skéllinaz` framing no longer looks current
- **2186 show / sċēawian** — mark older show-family `DEV_NOTES` debugging on `sc/sċ` normalization and `sċaweian` as superseded
- **2189 sieve / sife** — update `DEV_NOTES` §17.15 so it does not imply only `PROTOFORM` changed while legacy `PROTO *síbaz` remains the best reconstruction
- **2179 sheep / sċēap** — no `DEV_NOTES` or dossier cleanup needed

## Lane usage

- **All four memoed rows came from lane 4.**
- This batch used three note-bearing `regular` rows plus one `early_analogy` row.
- No refill outside the lane-4 pool was needed.

## Systematic workflow issues noticed

- **Dialect-normalized targets still need explicit note wording even when the derivation is fully regular.** `sheep` is stable only once the report states clearly that the project is targeting normalized WS `sċēap`, not every dialectal variant.
- **Some fixed rows still carry stale development-history headings that packets can over-weight.** `shilling` and `show` both illustrate how resolved debugging history can continue to look live unless `DEV_NOTES` is pruned or relabelled.
- **Legacy comparative `PROTO` values can lag behind OE-facing fixes.** `sieve` now has the right `PROTOFORM` and target, but the comparative-headword column still appears to preserve older bookkeeping rather than the best current reconstruction.

Batch 27 is complete. The rollout continues to the next four-row round.
