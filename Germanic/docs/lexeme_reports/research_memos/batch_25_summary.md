# Batch 25 summary

## Rows memoed

1. **2134 neck / hnecca** — **Lane 4** (`early_analogy`)
2. **2136 needle / nǣdl** — **Lane 4** (`early_analogy`)
3. **2138 net / nett** — **Lane 4** (`regular`)
4. **2141 nightmare / mare** — **Lane 4** (`regular`)

## Ready for final report

- **Ready:** 2136 needle / nǣdl; 2138 net / nett; 2141 nightmare / mare
- **Not yet ready:** 2134 neck / hnecca

## Rows needing paradigm probes

- **Needs paradigm probe:** none
- **No paradigm probe needed:** 2134 neck / hnecca; 2136 needle / nǣdl; 2138 net / nett; 2141 nightmare / mare

## Rows recommending TSV changes

- **2134 neck / hnecca** — TSV `PROTO`, TSV `NOTE`
- **2136 needle / nǣdl** — TSV `NOTE`
- **2138 net / nett** — TSV `NOTE`

## Rows recommending DEV_NOTES or dossier cleanup

- **2134 neck / hnecca** — update `DEV_NOTES` so it no longer implies both `PROTO` and `PROTOFORM` are `*xnekkô`; older `*xnakkăz` diagnostics should be marked historical
- **2136 needle / nǣdl** — mark the old protoform-fix section as implemented history rather than a live to-do
- **2138 net / nett** — refresh stale `DEV_NOTES` wording that still says TSV expects `net`
- **2141 nightmare / mare** — no row-level `DEV_NOTES` or dossier cleanup required

## Lane usage

- **All remaining rows are still lane 4 rows.**
- This batch was filled from two `early_analogy` rows plus two note-bearing `regular` rows.
- No refill outside the lane-4 pool was needed.

## Systematic workflow issues noticed

- **A few remaining `early_analogy` rows still hide weak comparative-headword metadata.** `neck / hnecca` is the current clearest case: the OE-facing input is solid, but the live cognate-set `PROTO` is no longer a good summary of the philology.
- **Late-rollout rows are increasingly about keeping comparative headwords separate from OE-facing inputs.** `needle` and `net` were both derivationally stable; the memo work mainly prevents stale or simplified metadata from being mistaken for the core analysis.
- **Stale packet-era lexical-table normalizations remain a recurring nuisance.** This batch again had weaker `net`/`mære` style materials that are useful only as historical context, not as current row authority.

Batch 25 is complete. The rollout continues to the next four-row round.
