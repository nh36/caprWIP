# Batch 33 summary

## Rows memoed

1. **2266 wade / wadan** — **Lane 4** (`regular`)
2. **2268 wake / wacan** — **Lane 4** (`early_analogy`)
3. **2270 warp / weorpan** — **Lane 4** (`regular`)
4. **2272 wash / wascan** — **Lane 4** (`regular`)

## Ready for final report

- **Ready:** 2266 wade / wadan; 2268 wake / wacan; 2270 warp / weorpan; 2272 wash / wascan

## Rows needing paradigm probes

- **Needs paradigm probe:** none
- **No paradigm probe needed:** 2266 wade / wadan; 2268 wake / wacan; 2270 warp / weorpan; 2272 wash / wascan

## Rows recommending TSV changes

- **2268 wake / wacan** — TSV `NOTE`
- **2270 warp / weorpan** — TSV `NOTE`

## Rows recommending DEV_NOTES or dossier cleanup

- **2266 wade / wadan** — optional `DEV_NOTES` cleanup to mark older `-ăną` / `wæden` recommendations as superseded
- **2268 wake / wacan** — minor `DEV_NOTES` cleanup so stale `*wakēną` / `*wakăną` stages are clearly superseded
- **2272 wash / wascan** — `DEV_NOTES` cleanup to mark older `-ăną` / `wasċan` expectations as superseded
- **2270 warp / weorpan** — no `DEV_NOTES` or dossier cleanup beyond the note clarification

## Lane usage

- **All four memoed rows came from lane 4.**
- This batch used three note-bearing `regular` rows plus one `early_analogy` row.
- No refill outside the lane-4 pool was needed.

## Systematic workflow issues noticed

- **Several late rollout rows are now mainly about row-boundary hygiene.** `warp` needed a firm separation from neighboring `wearp` material, while `wake` needed the weak/strong split kept explicit.
- **Much of the remaining cleanup burden is historical narrative, not data structure.** `wade` and `wash` were both stable rows whose only live issue was superseded `DEV_NOTES` history.
- **The comparative-proto vs OE-facing-input distinction continues to matter even in rows that are otherwise settled.** `wake` remains the clearest example in this batch.

Batch 33 is complete. The rollout continues to the next four-row round.
