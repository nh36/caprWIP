# Reviewer checklist result — ten / tēon

**Overall result:** pass with caveat.

## Genre and scope

| Item | Result | Note |
| :--- | :--- | :--- |
| Book-style entry about the word | yes | The prose centers on the numeral and its un-umlauted vs umlauted OE branches. |
| Avoids project autobiography | yes | Local files appear only in the support documents. |
| Linguistic rather than repository history | yes | The body prose is about numeral history and OE variant evidence. |
| Length fits the case | yes | The entry stays compact for an `attested_variant` row. |

## Evidence and forms

| Item | Result | Note |
| :--- | :--- | :--- |
| Gives actual source forms | yes | `tēon`, `tien/tīen`, `tēn`, `tēo`, `tēa`, `tēoða`, and `-tēontig` are named directly. |
| Avoids vague disagreement claims | yes | The branch split is stated with actual forms and sources. |
| Distinguishes attested / normalized / reconstructed / selected forms | yes | Exact `tēon` is described as a normalized comparison form rather than a straightforward dictionary headword. |
| Establishes OE evidence before comparison | yes | `Old English evidence` precedes `Variant comparison`. |
| Reconstructed OE target flagged if needed | yes | The entry explicitly says that `tēon` is a normalized comparison form. |

## Citations

| Item | Result | Note |
| :--- | :--- | :--- |
| Citation keys present in `docs/refs.bib` | yes | `Campbell1959`, `Fulk2018`, `SieversBrunner1965`. |
| Sources cited in prose | yes | No local file names appear in the model entry. |
| Missing-key issues recorded outside final prose | yes | No missing-key issue remains in the final prose. |

## Claims and certainty

| Item | Result | Note |
| :--- | :--- | :--- |
| Documented vs inferred outputs distinguished | yes | Trace-supported `tēon` is separated from directly attested simplex spellings. |
| Manual comparison labeled | yes | `Variant comparison` is identified as manual. |
| Avoids overstating certainty | yes | The entry does not present exact simplex `tēon` as the ordinary directly cited headword. |
| Avoids invented forms or probes | yes | No unsupported probe is claimed. |

## Style and process

| Item | Result | Note |
| :--- | :--- | :--- |
| No conclusion section | yes | The entry ends on the comparison table. |
| Avoids project-facing language after metadata | yes | The prose remains lexical and philological. |
| Implementation report records checks and scope | yes | Covered in the paired implementation report. |

## Remaining human-review points

1. The strongest direct simplex attestations are `tien/tīen`, `tēn`, `tēo`, and
   `tēa`, so exact `tēon` must remain labelled as a normalized comparison form.
2. Review should confirm that the live `attested_variant` label remains the best
   descriptor for a comparator whose support is strongest in derivatives such as
   `tēoða` and `-tēontig`.
