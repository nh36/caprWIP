# Reviewer checklist result — 2070 helm / helm

**Overall result:** pass with caveat.

## Genre and scope

| Item | Result | Note |
| :--- | :--- | :--- |
| Book-style entry about the word | yes | The prose stays centered on the helmet noun. |
| Avoids project autobiography | yes | Old debugging residue stays out of the model entry. |
| Linguistic rather than repository history | yes | The entry discusses comparative headwording, OE evidence, and development only. |
| Length fits the case | yes | It remains brief for a regular noun with one lexical caution. |

## Evidence and forms

| Item | Result | Note |
| :--- | :--- | :--- |
| Gives actual source forms | yes | `*helma-`, `*xélmaz`, `helm`, and `helma` are named directly. |
| Avoids vague disagreement claims | yes | The helmet-versus-rudder split is stated directly. |
| Distinguishes attested / normalized / reconstructed / selected forms | yes | Comparative headword, selected input, and separate OE lexeme are kept distinct. |
| Establishes OE evidence before comparison | yes | Dictionary evidence precedes the development summary. |
| Reconstructed OE target flagged if needed | n/a | The target `helm` is attested. |

## Citations

| Item | Result | Note |
| :--- | :--- | :--- |
| Citation keys present in `docs/refs.bib` | yes | `BosworthToller1898`, `ClarkHall1960`, `Kroonen2013`. |
| Sources cited in prose | yes | No local file names appear in the model entry. |
| Missing-key issues recorded outside final prose | yes | Memo-only source audit remains outside final prose. |

## Claims and certainty

| Item | Result | Note |
| :--- | :--- | :--- |
| Documented vs inferred outputs distinguished | yes | Comparative `*helma-` is separated from OE `helm` and from separate `helma`. |
| Manual comparison labeled | n/a | No paradigm comparison is needed. |
| Avoids overstating certainty | yes | The entry does not treat `helma` as a paradigm rival of helmet `helm`. |
| Avoids invented forms or probes | yes | No stale debug output appears in final prose. |

## Style and process

| Item | Result | Note |
| :--- | :--- | :--- |
| No conclusion section | yes | The entry ends after the form note. |
| Avoids project-facing language after metadata | yes | The body remains philological. |
| Implementation report records checks and scope | yes | Covered in the paired implementation report. |

## Remaining human-review points

1. Keep helmet `helm` distinct from rudder `helma`.
2. Keep comparative `*helma-` explicit as headword notation rather than an instruction to retarget the OE form.

## Citation-locator conditional-source pass

- Added page-specific locators for `BosworthToller1898, 542`.
- This pass was limited to claim-by-claim localization for `Campbell1959`, `BosworthToller1898`, and `Luick1914`.
- `KlugeSeebold2011` remained unchanged because the local text still does not preserve a reliable page marker.
