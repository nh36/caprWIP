# Reviewer checklist result — 2152 rest / ræste

**Overall result:** pass with caveat.

## Genre and scope

| Item | Result | Note |
| :--- | :--- | :--- |
| Book-style entry about the word | yes | The prose centers on the noun, its OE oblique form, and the paradigmatic split. |
| Avoids project autobiography | yes | Local files appear only in the support documents. |
| Linguistic rather than repository history | yes | The entry discusses the ō-stem paradigm and OE attestation. |
| Length fits the case | yes | The entry stays compact for a focused late-analogy case. |

## Evidence and forms

| Item | Result | Note |
| :--- | :--- | :--- |
| Gives actual source forms | yes | `*rastō`, `*rástōz`, `ræst`, and `ræste` are named directly. |
| Avoids vague disagreement claims | yes | The entry states the nominative-versus-oblique split explicitly. |
| Distinguishes attested / normalized / reconstructed / selected forms | yes | Attested `ræst` and `ræste` are separated from reconstructed `*rastō` and `*rástōz`. |
| Establishes OE evidence before comparison | yes | `Old English evidence` precedes `Paradigm comparison`. |
| Reconstructed OE target flagged if needed | n/a | `ræste` is treated as an attested OE form. |

## Citations

| Item | Result | Note |
| :--- | :--- | :--- |
| Citation keys present in `docs/refs.bib` | yes | `BosworthToller1898`, `ClarkHall1960`, `Kroonen2013`. |
| Sources cited in prose | yes | No local file names appear in the model entry. |
| Missing-key issues recorded outside final prose | yes | The compact trace and local paradigm synthesis remain in the support files only. |

## Claims and certainty

| Item | Result | Note |
| :--- | :--- | :--- |
| Documented vs inferred outputs distinguished | yes | The table distinguishes expected `rast` from documented trace output `ræste`. |
| Manual comparison labeled | yes | `Paradigm comparison` is identified as manual. |
| Avoids overstating certainty | yes | The entry keeps the selected genitive-singular framing modest and does not claim exclusivity for that one oblique cell. |
| Avoids invented forms or probes | yes | No unrun automatic probe is claimed. |

## Style and process

| Item | Result | Note |
| :--- | :--- | :--- |
| No conclusion section | yes | The entry ends with the comparison table. |
| Avoids project-facing language after metadata | yes | The body prose remains lexical and philological. |
| Implementation report records checks and scope | yes | Covered in the paired implementation report. |

## Cleanup note

- Post-audit cleanup pass 01 recast the flagged formulaic final-prose wording in
  the paired model entry without changing the analysis, citations, selected
  input, target form, classification, or comparison tables.

## Remaining human-review points

1. The entry still depends on a manual paradigm comparison and would benefit from
   a saved probe covering nominative and oblique singular cells.
2. A human reader should confirm that the present wording gives the right weight
   to the specifically genitive label preserved in the current source stack,
   since the broader oblique singular also converges on `ræste`.

## Citation-locator conditional-source pass

- Added page-specific locators for `BosworthToller1898, 121`.
- This pass was limited to claim-by-claim localization for `Campbell1959`, `BosworthToller1898`, and `Luick1914`.
- `KlugeSeebold2011` remained unchanged because the local text still does not preserve a reliable page marker.
