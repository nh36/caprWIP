# Reviewer checklist result — 2309 make (iptv.2sg) / maca

**Overall result:** pass with caveat.

## Genre and scope

| Item | Result | Note |
| :--- | :--- | :--- |
| Book-style entry about the word | yes | The prose centers on the make-family and the selected imperative cell. |
| Avoids project autobiography | yes | Local debugging history is kept out of the model entry. |
| Linguistic rather than repository history | yes | The argument is about class-II weak morphology and the finite-cell comparison. |
| Length fits the case | yes | The entry stays short enough for a specialized paradigm-cell row. |

## Evidence and forms

| Item | Result | Note |
| :--- | :--- | :--- |
| Gives actual source forms | yes | `*makōną`, `*mákô`, `macian`, `maca`, and `macaþ` are all named directly. |
| Avoids vague disagreement claims | yes | The entry states clearly that the headword and the selected finite cell belong to different levels of the same paradigm. |
| Distinguishes attested / normalized / reconstructed / selected forms | yes | The dictionary headword `macian` is separated from the selected imperative form `maca` and from reconstructed Germanic inputs. |
| Establishes OE evidence before comparison | yes | `Old English evidence` precedes `Paradigm comparison`. |
| Reconstructed OE target flagged if needed | yes | The entry treats `maca` as a selected paradigm form rather than as the dictionary citation form. |

## Citations

| Item | Result | Note |
| :--- | :--- | :--- |
| Citation keys present in `docs/refs.bib` | yes | `Campbell1959`, `ClarkHall1960`, `RingeTaylor2014`. |
| Sources cited in prose | yes | No local file names appear in the model entry. |
| Missing-key issues recorded outside final prose | yes | The compact trace and DEV_NOTES verification stay in the support documents only. |

## Claims and certainty

| Item | Result | Note |
| :--- | :--- | :--- |
| Documented vs inferred outputs distinguished | yes | The entry distinguishes the compact-trace output `maca` from the comparative headword `macian`. |
| Manual comparison labeled | yes | `Paradigm comparison` is identified as manual. |
| Avoids overstating certainty | yes | The prose does not claim that `maca` replaces the dictionary headword. |
| Avoids invented forms or probes | yes | No unattested extra cells are introduced beyond the clearly labeled companion form `macaþ`. |

## Style and process

| Item | Result | Note |
| :--- | :--- | :--- |
| No conclusion section | yes | The entry ends with the comparison table. |
| Avoids project-facing language after metadata | yes | The body prose remains philological. |
| Implementation report records checks and scope | yes | Covered in the paired implementation report. |

## Remaining human-review points

1. The row remains review-sensitive because `maca` is a selected finite paradigm
   form, while the ordinary dictionary headword is `macian`.
2. Later promotion should keep the distinction between the shared make-family
   evidence and the row-specific imperative-cell comparison explicit.

Citation locator claim-isolation 01 split the class-II background sentence and added verified locators for `Campbell1959, §159` and `RingeTaylor2014, 191`.

Citation locator claim-isolation 02 tightened the remaining broad claims and added verified locators for `RingeTaylor2014, 314` and `Campbell1959, §159`.
