# Reviewer checklist result — 1958 both / bū

**Overall result:** pass, with minor human-review points noted at the end.

## Genre and scope

| Item | Result | Note |
| :--- | :--- | :--- |
| Does the entry read like a book entry about the word? | yes | The prose centers on the OE dual form `bū` and its comparative setting. |
| Does it avoid project autobiography? | yes | Local files are confined to the ledger and implementation report. |
| Is the prose about linguistic history rather than repository history? | yes | The entry discusses reconstruction, OE evidence, development, and related forms. |
| Is the entry as short as the case permits, rather than inflated to match a harder entry? | yes | It stays compact and avoids analogical-case scaffolding. |

## Evidence and forms

| Item | Result | Note |
| :--- | :--- | :--- |
| Does the entry give actual source forms? | yes | It names `*bō`, `bū`, `bēġen`, `bā`, `báðir`, and `beide`. |
| Does it avoid vague statements such as “the handbooks disagree” unless the forms are immediately named? | yes | The reconstruction section names the older `*bō-jen-` and competing extended forms directly. |
| Does it distinguish attested, normalized, reconstructed, and project-selected forms? | yes | `bū`, `bēġen`, and `bā` are treated as attested OE forms; `*bō` is the inherited input; the extended forms are treated as related but different. |
| Does it establish the Old English evidence before the paradigm comparison? | yes | The OE evidence section comes before the `Form comparison`. |
| If an OE target is reconstructed, is that stated explicitly? | n/a | `bū` is treated as an attested OE form. |

## Citations

| Item | Result | Note |
| :--- | :--- | :--- |
| Do all citations use bibliography keys present in `docs/refs.bib`? | yes | `Campbell1959`, `Fulk2018`, `Kroonen2013`, `Orel2003`, and `SieversBrunner1965` were checked. |
| Are sources cited in prose rather than hidden behind local file names? | yes | The model entry cites named sources only. |
| If a cited source lacks a key, is that problem recorded in the ledger or implementation report rather than silently patched over? | yes | The bad Wiktionary extraction remains confined to the ledger and implementation report. |

## Claims and certainty

| Item | Result | Note |
| :--- | :--- | :--- |
| Does the entry distinguish documented outputs from inferred/expected outputs? | yes | The prose distinguishes source-backed forms from the manual comparison table and keeps the trace as supporting evidence. |
| If a comparison is manual, is it labeled as manual? | yes | The `Form comparison` section states this explicitly. |
| Does the entry avoid overstating certainty? | yes | The `bēġen` reconstruction debate is presented as unresolved background rather than as settled fact. |
| Does it avoid inventing unattested forms or automatic probe results? | yes | No unattested OE form or unrun automatic probe is claimed. |

## Style

| Item | Result | Note |
| :--- | :--- | :--- |
| Does it avoid repetitive “not X but Y” rhetorical negation? | yes | The prose uses direct positive statements. |
| Does it avoid generic summary endings? | yes | There is no conclusion section. |
| Does it avoid a conclusion unless there is a strong reason to include one? | yes | The entry ends with `Form comparison`. |
| Does it avoid unnecessary project-facing terms after the brief metadata section? | yes | The entry remains in historical-linguistic prose after the table. |

## Forbidden project-facing content in the book-style prose

| Item | Result | Note |
| :--- | :--- | :--- |
| Does it avoid mentioning packets, DEV_NOTES, backlog, manifest, implementation log, or project history in the final entry prose? | yes | Those appear only in the supporting files. |
| Are local tools discussed only in the ledger and implementation report? | yes | The final entry does not narrate them. |

## Process record

| Item | Result | Note |
| :--- | :--- | :--- |
| Does the implementation report state what was checked? | yes | Files inspected, citation checks, and the manual form comparison are recorded. |
| Does the implementation report state what was not checked, when relevant? | yes | It states that no automatic form probe was run and notes unresolved uncertainties. |
| Does the implementation report confirm that no TSV, FST, manifest, packet, memo, bibliography, derivation trace, or pilot report was changed? | yes | Scope confirmation is explicit. |

## Remaining human-review points

1. The analysis of `bēġen` remains the main philological uncertainty: older
   `*bō-jen-` accounts coexist with Kroonen's analogical analysis and Seebold's
   `*bō-þ-` objection.
2. Campbell's OCR prints `bā` in the final-accented `ō` example set, whereas the
   broader local evidence supports `bū` as the intended neuter example.
