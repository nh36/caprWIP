# Reviewer checklist result — 2310 make (3sg) / macaþ

**Overall result:** pass with caveat.

## Genre and scope

| Item | Result | Note |
| :--- | :--- | :--- |
| Book-style entry about the word | yes | The prose centers on the make-family and the selected 3sg cell. |
| Avoids project autobiography | yes | Local debugging history stays out of the model entry. |
| Linguistic rather than repository history | yes | The argument is about class-II morphology and the finite-cell comparison. |
| Length fits the case | yes | The entry stays compact for a single paradigm-cell row. |

## Evidence and forms

| Item | Result | Note |
| :--- | :--- | :--- |
| Gives actual source forms | yes | `*makōjaną`, `*mákōθi`, `macian`, `maca`, and `macaþ` are all named directly. |
| Avoids vague disagreement claims | yes | The entry states plainly that the headword and the selected 3sg cell are different levels of the same paradigm. |
| Distinguishes attested / normalized / reconstructed / selected forms | yes | `macian` is separated from normalized `macaþ` and reconstructed Germanic inputs. |
| Establishes OE evidence before comparison | yes | `Old English evidence` precedes `Paradigm comparison`. |
| Reconstructed OE target flagged if needed | yes | `Macaþ` is presented as the normalized comparison form rather than as the dictionary headword. |

## Citations

| Item | Result | Note |
| :--- | :--- | :--- |
| Citation keys present in `docs/refs.bib` | yes | `Campbell1959`, `ClarkHall1960`, `Kroonen2013`, `RingeTaylor2014`. |
| Sources cited in prose | yes | No local file names appear in the model entry. |
| Missing-key issues recorded outside final prose | yes | Luick and the trace material remain in the support documents only. |

## Claims and certainty

| Item | Result | Note |
| :--- | :--- | :--- |
| Documented vs inferred outputs distinguished | yes | The table distinguishes the trace output `macaþ` from the lemma comparator `macian`. |
| Manual comparison labeled | yes | The comparison section is explicitly identified as manual. |
| Avoids overstating certainty | yes | The prose does not claim that `macaþ` replaces the ordinary headword. |
| Avoids invented forms or probes | yes | Only the lemma and the directly relevant companion forms are used. |

## Style and process

| Item | Result | Note |
| :--- | :--- | :--- |
| No conclusion section | yes | The entry ends with the comparison table. |
| Avoids project-facing language after metadata | yes | The body prose remains philological. |
| Implementation report records checks and scope | yes | Covered in the paired implementation report. |

## Remaining human-review points

1. The row remains review-sensitive because `macaþ` is a normalized finite paradigm form, while the ordinary dictionary headword is `macian`.
2. Later promotion should keep the distinction between shared make-family evidence and the row-specific 3sg comparison explicit.
