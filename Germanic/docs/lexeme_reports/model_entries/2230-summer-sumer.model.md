# summer — OE sumer

PROTO: *súmaraz
PROTOFORM: *súmaraz
COUNTERPART: sumer
DERIVATION_CLASS: regular

### Transducer input and output

| Item | Value |
| :--- | :--- |
| lexical item | summer |
| citation reconstruction / lexeme label | `*sumara- / *sumaraz` |
| selected input form | `*súmaraz` |
| Old English target | `sumer` |
| classification | regular |
| documented output | `*súmaraz -> sumer` |

### Reconstruction and comparative evidence

Kroonen gives the lexeme as `*sumara-`, and Ringe and Taylor likewise use [sumaraz]{.recon} ‘summer’, while Orel preserves an alternate [sumeraz]{.recon} ‘summer’ [@Kroonen2013; @RingeTaylor2014; @Orel2003, 425]. The selected input [súmaraz]{.recon} ‘summer’ follows the `*a` vocalism that underlies the regular development represented here.

### Old English evidence

Clark Hall gives `sumor m., gs. sumeres, ds. sumera, sumere`, and Bright likewise lists `sumor (sumer)` with genitive `sumeres` 'summer' [@ClarkHall1960; @BrightCassidyRingler1971, 440]. The tradition therefore preserves both `sumor` 'summer' and `sumer` 'summer', with the oblique forms strongly supporting second-syllable `e`.

### Development to Old English

From [súmaraz]{.recon} ‘summer’, loss of final `-az` is followed by fronting and merger in the unstressed second syllable, yielding `sumer` 'summer'. The selected form is the regularized `e`-form, while the common citation form `sumor` 'summer' remains part of the attested OE tradition [@RingeTaylor2014].

### Form note

The entry does not deny `sumor` 'summer'. It represents `sumer` 'summer' as the regular outcome chosen here, while `sumor` remains a common headword spelling and `sumeres/sumere` show that the `e`-vocalism was also real in Old English [@ClarkHall1960; @BrightCassidyRingler1971, 440].
