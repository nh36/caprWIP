# Reviewer checklist result — 2162 rust / rust

**Overall result:** pass with caveat.

## Genre and scope

| Item | Result | Note |
| :--- | :--- | :--- |
| Book-style entry about the word | yes | The prose centers on the noun, its attestation, and the unresolved vowel history. |
| Avoids project autobiography | yes | Local files appear only in the support documents. |
| Linguistic rather than repository history | yes | The entry discusses comparative reconstruction, OE evidence, and the unresolved exception. |
| Length fits the case | yes | The entry stays compact for an unexplained case. |

## Evidence and forms

| Item | Result | Note |
| :--- | :--- | :--- |
| Gives actual source forms | yes | `*rústō`, `*rustaz`, `rost`, `rust`, `rūst`, and `rúst` are named directly. |
| Avoids vague disagreement claims | yes | The entry states the competing citation reconstruction and the unresolved vowel problem explicitly. |
| Distinguishes attested / normalized / reconstructed / selected forms | yes | Attested OE spellings are separated from the competing reconstructed PGmc forms and the computed regular outputs. |
| Establishes OE evidence before comparison | yes | `Old English evidence` precedes `Expected and attested forms`. |
| Reconstructed OE target flagged if needed | n/a | `rust` is an attested OE noun. |

## Citations

| Item | Result | Note |
| :--- | :--- | :--- |
| Citation keys present in `docs/refs.bib` | yes | `BosworthToller1898`, `Campbell1959`, `ClarkHall1960`, `Orel2003`, `RingeTaylor2014`. |
| Sources cited in prose | yes | No local file names appear in the model entry. |
| Missing-key issues recorded outside final prose | yes | The local comparator set remains in the ledger and implementation report only. |

## Claims and certainty

| Item | Result | Note |
| :--- | :--- | :--- |
| Documented vs inferred outputs distinguished | yes | The table distinguishes computed `rost`, expected `rost`, the negative-control `*rústu -> rust`, and attested `rust`. |
| Manual comparison labeled | yes | `Expected and attested forms` is identified as manual. |
| Avoids overstating certainty | yes | The entry states plainly that the citation-form development remains unexplained. |
| Avoids invented forms or probes | yes | No rescue analysis is presented as accepted. |

## Style and process

| Item | Result | Note |
| :--- | :--- | :--- |
| No conclusion section | yes | The entry ends with the comparison table. |
| Avoids project-facing language after metadata | yes | The body prose remains lexical and philological. |
| Implementation report records checks and scope | yes | Covered in the paired implementation report. |

## Remaining human-review points

1. This entry should not be scaled as a repair model, because its central result
   is that the citation-form development remains unexplained.
2. A human reader should confirm how prominently the competing citation
   reconstruction `*rustaz` should stand beside the preserved header form
   `*rústō` in any later promotion pass.

## Citation-locator conditional-source pass

- Added page-specific locators for `BosworthToller1898, 677`.
- This pass was limited to claim-by-claim localization for `Campbell1959`, `BosworthToller1898`, and `Luick1914`.
- `KlugeSeebold2011` remained unchanged because the local text still does not preserve a reliable page marker.

## Citation-locator claim-isolation 04

- Citation locator tightened after claim-isolation pass; verified against `ClarkHall1960, 245`.
